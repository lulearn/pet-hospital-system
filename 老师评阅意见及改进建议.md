# 老师评阅意见及改进建议

---

## 总体评价

这位同学，项目整体完成度不错，三种角色的业务流程基本跑通了，前后端分离的架构也搭起来了。但你如果站在答辩的角度，我作为评阅老师会提以下问题和改进建议。有些是你答辩时会被追问的，有些是报告里应该补充的。下面我按技术层面和业务层面分开说。

---

## 一、技术层面问题

### 1. 密码加密为什么还在用 MD5？

这是答辩时必被问的问题。你的报告里写了"MD5 有一定局限性"，但在代码里从头用到尾。老师会问：

- MD5 有什么具体的安全风险？（彩虹表攻击）
- 为什么不用 BCrypt 或至少加盐（Salt）？
- 如果让你升级加密方案，代码要改哪些地方？

实际上 Spring Security 自带了 BCryptPasswordEncoder，改起来并不难。建在代码里留一个加密工具类的接口，方便以后替换。

### 2. 为什么不用 Spring Security？

你的 JWT 拦截器是手写的，功能是实现了，但老师会问：

- Spring Security 是业界标准的安全框架，为什么不用？
- 手写的拦截器和 Spring Security 的 Filter Chain 有什么区别？
- 如果后续要加 OAuth2、Remember-Me、Session 并发控制，你的手写方案能扩展吗？

手动实现的好处是代码简单、你自己能讲清楚每一行逻辑。但答辩时一定要能解释清楚"为什么选择手动实现而不是用框架"——不是因为不会，而是因为项目规模小，手动实现更轻量、逻辑透明、便于教学展示。

### 3. 代码里没有事务管理

这是比较严重的技术问题。你的订单流程——检查库存、扣减库存、创建订单——这三个操作应该在一个数据库事务里，要么全成功，要么全回滚。当前代码没有显式的 `@Transactional` 注解。老师会追问：

- 如果扣了库存但创建订单失败了怎么办？
- Spring Boot 的默认事务传播机制是什么？
- MyBatis-Plus 的每个操作是独立事务吗？

**建议**：在 `addOrder` 方法上加 `@Transactional`，在报告里补一段事务管理的说明。

### 4. 没有单元测试

你的测试章节只有手工功能测试，没有一行自动化测试代码。老师会问：

- 核心业务逻辑（如订单金额计算、余额扣减）有没有单元测试？
- 你怎么保证改了代码以后之前的功能不会坏？
- JUnit 和 Mockito 用过吗？

至少给关键的 Service 方法（login、addOrder、payOrder）写几个单元测试，报告里贴一段测试代码。

### 5. 分页功能完全缺失

所有列表接口都是全量返回，现在数据少看不出来，但这是设计层面的缺陷。老师会问：

- 如果有 10 万条预约记录，前端页面会怎样？
- MyBatis-Plus 的分页插件怎么配置？
- 前端的 Element Plus Pagination 组件怎么对接后端分页？

至少在后端配置好 MyBatis-Plus 的分页拦截器，在一两个接口里演示分页效果。

### 6. 后端没有输入校验

你的 User 实体上有 `@NotBlank` 注解，但 Controller 方法参数上没有 `@Valid`，这些注解根本没生效。也就是说，当前系统允许用户名为空、密码为空、手机号格式错误的数据入库。老师会问：

- 后端怎么防止空数据写入数据库？
- `@Valid` 和 `@Validated` 的区别是什么？
- 如果前端被绕过了，后端有防线吗？

### 7. 没有 API 文档

40 多个接口，没有任何自动化文档。老师可能会说：

- 前端开发怎么知道接口的入参和返回格式？
- Swagger / Knife4j 集成过吗？
- 接口文档在团队协作中为什么重要？

集成 Knife4j 只需要加一个依赖和一个注解，建议加上。

### 8. 前端没有用 TypeScript

Vue 3 官方推荐 TypeScript，你的项目全用 JavaScript。老师不一定扣分，但会说：

- TypeScript 相比 JavaScript 有什么优势？
- 项目中变量类型不明确，长期维护会有什么问题？

### 9. 没有日志系统

现在只有 MyBatis 的 SQL 日志输出到控制台。没有业务日志（谁在什么时候做了什么）、没有错误日志分级、没有日志持久化。这是一个管理系统的基础要求。

### 10. 软删除为什么不用 MyBatis-Plus 自带的 @TableLogic？

MyBatis-Plus 从 3.3 版本开始就提供了 `@TableLogic` 注解，配置后自动在查询和删除时处理逻辑删除。你是手动加条件 `.ne(deleted, 1)` 做的，功能一样但代码更冗余。老师会问你对 MyBatis-Plus 的了解是否透彻。

---

## 二、业务层面问题

### 1. 预约没有时间冲突检测

用户可以选择任意时间预约任意医生，系统不会检查该医生在该时段是否已被其他人预约。这是一个核心业务逻辑的缺失。老师会问：

- 如果一个医生同一天被约了 10 次会发生什么？
- 你怎么实现时间冲突检测？
- 前端是否应该根据已占用的时间禁用某些时段？

### 2. 没有取消预约和退款流程

用户只能删除待审核的预约，但如果预约已通过，用户想取消怎么办？已支付的订单能不能退款？你的系统只支持正向流程，缺少逆向（取消、退款）流程。老师会问：

- 真实业务场景中，用户取消预约怎么处理？
- 已支付订单退款后，余额怎么恢复？库存要不要回补？

### 3. 药品库存扣减的并发安全问题

当前代码的库存扣减逻辑存在并发隐患：如果两个用户同时购买最后一件药品，两个请求都查到库存为 1，都判断通过，然后都扣减，最终库存变成 -1。老师一定会问这个问题：

- 高并发下扣库存怎么保证安全？
- 乐观锁和悲观锁的区别？这个场景用哪个？
- 数据库行级锁（SELECT ... FOR UPDATE）了解吗？

这是答辩中几乎必问的问题，你一定要准备好。

### 4. 没有权限的细粒度控制

管理员的权限是"全部开放"，不能创建子管理员、不能限制某些管理员只能管理药品而不能管理用户。老师会说：

- 如果有多个管理员，怎么分配不同的管理权限？
- RBAC（基于角色的访问控制）模型了解吗？

### 5. 没有数据导出功能

管理系统基本都需要导出报表——预约记录导出 Excel、订单汇总导出、收入统计等。你的系统完全没有导出功能，老师会认为"管理系统"这一块考虑不周全。

### 6. 一人多宠物场景没有覆盖

当前预约时手动填写宠物名，但宠物和用户之间没有独立的宠物表。如果一个用户有两只狗（旺财和来福），系统无法区分它们的就诊历史。老师会问：

- 为什么宠物不单独建一张表？
- 宠物是否有品种、年龄、性别等字段？
- 如何查看某只宠物的所有就诊历史？

### 7. 首页统计数据缺乏图表展示

三端首页都只有数字卡片，没有图表。Element Plus 本身不提供图表，但可以集成 ECharts。老师会说：

- 医生工作量趋势怎么看？
- 药品销量排名怎么看？
- 为什么不加几个简单的柱状图或饼图？

### 8. 医生没有排班机制

所有医生默认都是"可预约"状态，没有排班时间和休假管理。老师会问：

- 医生某天请假怎么办？
- 周末和节假日怎么处理？
- 是否需要医生的出诊时间段管理？

---

## 三、答辩时可能被追问的具体场景问题

以下问题老师可能会让你现场在白板上画流程或写伪代码：

1. **"两个用户同时买最后一瓶药，你怎么保证不出错？"** → 让你现场讲乐观锁或悲观锁的方案。

2. **"如果有人抓包拿到了 Token，冒充其他用户怎么办？"** → 让你讲 HTTPS、Token 过期、Refresh Token、IP 绑定等方案。

3. **"从登录到看到首页，你的代码经过了哪些层？"** → 考你对全链路流程的理解，从浏览器 → Vue Router 守卫 → Axios 拦截器 → Spring 拦截器 → Controller → Service → Mapper → MySQL。

4. **"如果数据库宕机了，系统会出现什么现象？怎么处理？"** → 考你对异常处理和容灾的理解。

5. **"你的系统怎么部署到服务器上？需要哪些步骤？"** → 考你对项目部署的了解程度（打包、Nginx 反向代理、HTTPS 配置等）。

---

## 四、给报告的修改建议

1. **补充系统架构图**：当前只有文字描述，建议用 draw.io 等工具画一张系统架构图、一张 ER 图、一张核心业务流程图。

2. **补充核心代码的流程图**：登录认证流程、预约状态流转、订单流程，各画一个流程图放报告里。

3. **代码片段加上注释说明**：报告中引用的代码片段应标注关键行号和说明文字，不要直接贴代码。

4. **测试用例表补充测试数据**：当前测试表里没有具体的测试数据（账户余额多少、药品库存多少），应补充具体数值。

5. **补充部署说明**：增加一个章节或附录，描述如何将项目打包部署到服务器。

6. **参考文献要真实可查**：当前参考文献部分是自己编的，答辩时老师可能会问某篇文献的核心观点是什么。要用自己真正读过的文献。

---

## 五、总结——优先级排序

如果时间有限，建议按这个顺序改：

| 优先级 | 问题 | 原因 |
|--------|------|------|
| 必改 | 加 @Transactional 事务 | 涉及数据一致性，答辩必被问 |
| 必改 | 补充单元测试（至少2-3个） | 老师非常看重 |
| 必改 | 画系统架构图 + ER 图 | 报告里一张图没有，观感很差 |
| 建议改 | 库存并发安全方案（至少能口头回答） | 答辩必问 |
| 建议改 | 加 @Valid 后端校验 | 体现安全意识 |
| 建议改 | 集成 Swagger/Knife4j | 只需加一个依赖 |
| 可选 | 密码升级 BCrypt | 工作量小，效果明显 |
| 可选 | 加分页 | 后端加拦截器，前端加分页组件 |
| 可选 | 加 ECharts 图表 | 首页视觉效果提升显著 |
