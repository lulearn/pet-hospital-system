# 宠物医院后台管理系统的设计与实现

## 摘要

随着人们生活水平的提高，宠物医疗需求日益增长。传统宠物医院依赖纸质记录和人工管理，存在效率低下、信息查询困难、数据易丢失等问题。本课题设计并实现了一套基于 Spring Boot + Vue 3 的宠物医院后台管理系统，支持用户（宠物主人）、医生、管理员三种角色的协同工作，涵盖在线预约、就诊管理、药品购买、订单支付、数据统计等核心功能。系统采用前后端分离架构，后端使用 Spring Boot 2.7 和 MyBatis-Plus 框架，前端使用 Vue 3 和 Element Plus 组件库，数据库采用 MySQL 8.0，认证方案采用 JWT 无状态令牌。系统实现了角色权限隔离、软删除数据保护、订单库存校验等关键机制，为宠物医院的信息化管理提供了一套完整解决方案。

**关键词**：宠物医院；管理系统；Spring Boot；Vue 3；前后端分离

---

## 第1章 绪论

### 1.1 课题背景

近年来，宠物经济蓬勃发展，宠物医疗作为刚性需求增长迅速。然而，许多中小型宠物医院仍然采用纸质记录或简单的电子表格进行管理，存在以下问题：

1. **预约管理混乱**：宠物主人通过电话或到店预约，医生无法实时了解预约情况，容易造成时间冲突。
2. **就诊记录难追溯**：纸质病历容易丢失，宠物主人下次就诊时，医生无法快速了解历史诊断信息。
3. **药品库存管理粗放**：手工记录库存容易出现差错，缺货或积压情况时有发生。
4. **订单和支付脱节**：购药流程不透明，付款和发货环节分离，容易产生纠纷。

因此，开发一套适合中小型宠物医院使用的信息化管理系统，具有实际应用价值。

### 1.2 课题意义

本课题的意义体现在以下几个方面：

1. **提高管理效率**：将预约、就诊、购药、支付等流程线上化，减少人工操作环节。
2. **保障数据安全**：采用软删除机制，避免误操作导致数据永久丢失；通过角色权限控制，防止越权操作。
3. **改善用户体验**：宠物主人可在线浏览医生、预约挂号、购买药品、查看就诊记录；医生可快速审核预约、录入诊断和处方。
4. **提供决策依据**：管理员可查看系统全局数据，了解医院运营状况。

### 1.3 国内外研究现状

在欧美国家，兽医诊所管理系统（如 eVetPractice、Vetter Software）已较为成熟，功能涵盖电子病历、预约排班、库存管理、在线支付等。国内宠物医疗信息化起步较晚，现有系统多面向大型连锁医院，中小型宠物医院因成本和技术门槛难以应用。本课题针对中小型宠物医院的日常管理需求，设计一套轻量级、易部署的管理系统。

---

## 第2章 需求分析

### 2.1 功能需求

系统面向三种角色，各自的功能需求如下。

#### 2.1.1 用户端（宠物主人）

| 功能模块 | 功能描述 |
|---------|---------|
| 注册与登录 | 填写用户名、密码、个人信息注册账号 |
| 首页看板 | 查看我的预约数、订单数、账户余额 |
| 医生浏览 | 查看医生列表，包括姓名、擅长领域、简介 |
| 预约挂号 | 选择医生和时间进行预约，填写宠物信息 |
| 预约管理 | 查看、修改、删除自己的预约 |
| 药品浏览 | 查看药品列表，了解价格和库存 |
| 订单管理 | 下单购买药品、支付订单、确认收货 |
| 就诊记录 | 查看医生给自己的诊断结果和处方 |
| 充值中心 | 在线充值账户余额 |
| 个人中心 | 修改个人信息、修改密码 |

#### 2.1.2 医生端

| 功能模块 | 功能描述 |
|---------|---------|
| 登录 | 使用医生账号登录 |
| 工作台 | 查看待处理预约数、就诊记录总数、已完成数 |
| 预约审核 | 查看分配给自己的预约，通过或拒绝 |
| 就诊管理 | 创建就诊记录、填写诊断和处方、完成就诊 |

#### 2.1.3 管理端

| 功能模块 | 功能描述 |
|---------|---------|
| 用户管理 | 查看、添加、编辑、删除用户 |
| 医生管理 | 查看、添加、编辑、删除医生 |
| 预约记录管理 | 查看所有预约记录，支持软删除 |
| 药品管理 | 查看、添加、编辑、删除药品 |
| 订单管理 | 查看所有订单，支持发货和删除 |
| 就诊记录管理 | 查看所有就诊记录，支持软删除 |

### 2.2 非功能需求

1. **安全性**：密码加密存储，接口需认证访问，角色权限隔离
2. **可用性**：界面清晰直观，操作流程简短，有二次确认提示
3. **可维护性**：代码结构清晰，前后端分离，便于后续扩展
4. **响应速度**：页面加载和数据交互在可接受时间内（2秒以内）

---

## 第3章 系统设计

### 3.1 系统架构

系统采用前后端分离的 B/S 架构，分为三层：

- **表现层**：Vue 3 + Element Plus，运行在浏览器中，负责页面展示和用户交互
- **业务逻辑层**：Spring Boot，运行在服务器上，处理业务逻辑，通过 RESTful API 与前端通信
- **数据持久层**：MyBatis-Plus + MySQL，负责数据的存取

```
浏览器（Vue 3 + Element Plus）
    ↕ HTTP / JSON
Spring Boot（Controller → Service → Mapper）
    ↕ SQL
MySQL 数据库
```

### 3.2 技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.18 | 后端框架，自动配置，内嵌 Tomcat |
| MyBatis-Plus | 3.5.3 | ORM 框架，简化数据库操作 |
| MySQL | 8.0 | 关系型数据库 |
| JWT (jjwt) | 0.11.5 | 无状态身份认证 |
| Vue 3 | 3.x | 前端框架，Composition API |
| Element Plus | 2.x | UI 组件库 |
| Vite | 5.x | 前端构建工具 |
| Vue Router | 4.x | 前端路由 |
| Pinia | 2.x | 前端状态管理 |
| Axios | 1.x | HTTP 请求库 |

### 3.3 数据库设计

数据库名为 `pet_hospital`，包含 6 张核心业务表。

#### 3.3.1 用户表 (user)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(100) | 密码，MD5 加密 |
| real_name | VARCHAR(50) | 真实姓名 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(50) | 邮箱 |
| role | VARCHAR(20) | 角色：USER/ADMIN |
| balance | DECIMAL(10,2) | 账户余额 |
| create_time | DATETIME | 创建时间 |

#### 3.3.2 医生表 (doctor)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| username | VARCHAR(50) | 用户名 |
| password | VARCHAR(100) | 密码，MD5 加密 |
| real_name | VARCHAR(50) | 真实姓名 |
| specialty | VARCHAR(100) | 擅长领域 |
| description | TEXT | 简介 |
| create_time | DATETIME | 创建时间 |

#### 3.3.3 药品表 (medicine)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| name | VARCHAR(100) | 药品名称 |
| description | TEXT | 药品描述 |
| price | DECIMAL(10,2) | 单价 |
| stock | INT | 库存数量 |
| create_time | DATETIME | 创建时间 |

#### 3.3.4 预约表 (appointment)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 用户 ID（外键） |
| doctor_id | BIGINT | 医生 ID（外键） |
| pet_name | VARCHAR(50) | 宠物名称 |
| species | VARCHAR(50) | 宠物种类 |
| description | TEXT | 病情描述 |
| status | VARCHAR(20) | 状态 |
| deleted | TINYINT | 软删除标记 |
| appointment_time | DATETIME | 预约时间 |
| create_time | DATETIME | 创建时间 |

status 取值：PENDING（待审核）、APPROVED（已通过）、REJECTED（已拒绝）、IN_PROGRESS（就诊中）、COMPLETED（已完成）

#### 3.3.5 就诊记录表 (consultation)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| doctor_id | BIGINT | 医生 ID |
| user_id | BIGINT | 用户 ID |
| appointment_id | BIGINT | 关联预约 ID |
| pet_name | VARCHAR(50) | 宠物名称 |
| diagnosis | TEXT | 诊断结果 |
| prescription | TEXT | 处方 |
| notes | TEXT | 备注 |
| status | VARCHAR(20) | 状态 |
| deleted | TINYINT | 软删除标记 |
| create_time | DATETIME | 创建时间 |

status 取值：IN_PROGRESS（就诊中）、COMPLETED（已完成）

#### 3.3.6 订单表 (orders)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 用户 ID |
| medicine_id | BIGINT | 药品 ID |
| quantity | INT | 购买数量 |
| total_price | DECIMAL(10,2) | 总价 |
| status | VARCHAR(20) | 状态 |
| pay_time | DATETIME | 支付时间 |
| ship_time | DATETIME | 发货时间 |
| receive_time | DATETIME | 收货时间 |
| create_time | DATETIME | 创建时间 |

status 取值：UNPAID（未支付）、PAID（已支付）、SHIPPED（已发货）、RECEIVED（已收货）

#### 3.3.7 实体关系图（E-R 图描述）

- 用户 1:N 预约（一个用户可以有多条预约）
- 医生 1:N 预约（一个医生被多次预约）
- 医生 1:N 就诊记录（一个医生有多条就诊记录）
- 用户 1:N 就诊记录（一个用户有多条就诊记录）
- 预约 1:1 就诊记录（一条预约对应一条就诊记录）
- 用户 1:N 订单（一个用户有多个订单）
- 药品 1:N 订单（一种药品对应多条订单）

### 3.4 API 接口设计

#### 3.4.1 认证模块

| 方法 | 路径 | 请求参数 | 返回数据 | 说明 |
|------|------|---------|---------|------|
| POST | /api/login | username, password, role | token, user | 登录 |
| POST | /api/register | username, password, ... | — | 注册 |

#### 3.4.2 用户端模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/doctors | 获取医生列表 |
| GET | /api/appointments | 获取我的预约 |
| POST | /api/appointments | 创建预约 |
| PUT | /api/appointments | 修改预约 |
| DELETE | /api/appointments/{id} | 删除预约 |
| GET | /api/medicines | 获取药品列表 |
| GET | /api/orders | 获取我的订单 |
| POST | /api/orders | 创建订单 |
| DELETE | /api/orders/{id} | 删除订单 |
| POST | /api/orders/{id}/pay | 支付订单 |
| POST | /api/orders/{id}/receive | 确认收货 |
| GET | /api/consultations | 获取我的就诊记录 |
| POST | /api/recharge | 充值 |
| GET | /api/user/profile | 获取个人信息 |
| PUT | /api/user/profile | 修改个人信息 |
| PUT | /api/user/change-password | 修改密码 |

#### 3.4.3 医生端模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/doctor/appointments | 获取分配给我的预约 |
| GET | /api/doctor/appointments/stats | 预约统计数据 |
| PUT | /api/doctor/appointments | 审核预约 |
| GET | /api/doctor/consultations | 获取我的就诊记录 |
| GET | /api/doctor/consultations/stats | 就诊统计数据 |
| POST | /api/doctor/consultations | 创建就诊记录 |
| PUT | /api/doctor/consultations | 修改就诊记录 |
| PUT | /api/doctor/consultations/{id}/complete | 完成就诊 |
| GET | /api/doctor/users | 获取用户列表 |
| GET | /api/doctor/profile | 获取个人信息 |
| PUT | /api/doctor/profile | 修改个人信息 |
| PUT | /api/doctor/change-password | 修改密码 |

#### 3.4.4 管理端模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | /api/admin/users | 用户管理 |
| GET/POST/PUT/DELETE | /api/admin/doctors | 医生管理 |
| GET/DELETE | /api/admin/appointments | 预约记录管理 |
| GET/POST/PUT/DELETE | /api/admin/medicines | 药品管理 |
| GET/DELETE | /api/admin/orders | 订单管理 |
| PUT | /api/admin/orders/{id}/ship | 发货 |
| GET/DELETE | /api/admin/consultations | 就诊记录管理 |
| GET/PUT | /api/admin/profile | 个人中心 |
| PUT | /api/admin/change-password | 修改密码 |

---

## 第4章 系统实现

### 4.1 开发环境

| 项目 | 说明 |
|------|------|
| 操作系统 | Windows 10 / Linux |
| JDK 版本 | 17.0.18 |
| 开发工具 | IntelliJ IDEA / VS Code |
| 数据库 | MySQL 8.0 |
| 构建工具 | Maven 3.8 / npm |
| 版本控制 | Git + GitHub |

### 4.2 后端核心实现

#### 4.2.1 项目结构

```
src/main/java/com/example/demo/
├── DemoApplication.java          # 启动类
├── common/                       # 通用组件
│   ├── Result.java               # 统一返回格式
│   └── GlobalExceptionHandler.java
├── config/WebConfig.java         # CORS + 拦截器配置
├── controller/                   # 控制器层
│   ├── AuthController.java       # 登录注册
│   ├── UserController.java       # 用户端
│   ├── DoctorController.java     # 医生端
│   └── AdminController.java      # 管理端
├── entity/                       # 实体类（6个）
├── mapper/                       # MyBatis-Plus Mapper（6个）
├── service/                      # 业务逻辑层
├── interceptor/JwtInterceptor.java  # JWT 拦截器
└── util/JwtUtil.java             # JWT 工具类
```

#### 4.2.2 认证与授权

JWT（JSON Web Token）认证流程：

1. 用户提交用户名、密码和角色登录
2. 后端校验成功后，生成包含 userId、username、role 的 JWT Token
3. Token 使用 HS256 算法签名，有效期 24 小时
4. 前端将 Token 存储在 localStorage，每次请求通过 Authorization Header 携带
5. JwtInterceptor 拦截所有 /api/** 请求，验证 Token 有效性，提取用户信息存入 request 属性
6. 拦截器同时检查路径前缀与角色是否匹配，实现接口级权限控制

密码加密采用 MD5 摘要算法，数据库中存储密文，不存储明文密码。

#### 4.2.3 预约状态流转实现

```
PENDING → APPROVED → IN_PROGRESS → COMPLETED
         ↘ REJECTED
```

- 用户创建预约 → status = PENDING
- 医生通过 → status = APPROVED
- 医生拒绝 → status = REJECTED
- 医生创建就诊记录 → 预约 status = IN_PROGRESS，就诊 status = IN_PROGRESS
- 医生完成就诊 → 预约 status = COMPLETED，就诊 status = COMPLETED

状态变更在 DoctorController 中实现，关键代码如下：

```java
// 添加就诊记录：预约状态同步为就诊中
@PostMapping("/consultations")
public Result<?> addConsultation(@RequestBody Consultation consultation) {
    consultation.setStatus("IN_PROGRESS");
    consultationMapper.insert(consultation);
    if (consultation.getAppointmentId() != null) {
        Appointment appt = appointmentMapper.selectById(consultation.getAppointmentId());
        appt.setStatus("IN_PROGRESS");
        appointmentMapper.updateById(appt);
    }
    return Result.success();
}

// 完成就诊：就诊和预约都标记为已完成
@PutMapping("/consultations/{id}/complete")
public Result<?> completeConsultation(@PathVariable Long id) {
    Consultation consultation = consultationMapper.selectById(id);
    consultation.setStatus("COMPLETED");
    consultationMapper.updateById(consultation);
    Appointment appt = appointmentMapper.selectById(consultation.getAppointmentId());
    appt.setStatus("COMPLETED");
    appointmentMapper.updateById(appt);
    return Result.success();
}
```

#### 4.2.4 订单流程实现

订单创建时进行库存校验和扣减：

```java
@PostMapping("/orders")
public Result<?> addOrder(@RequestBody Order order) {
    Medicine medicine = medicineMapper.selectById(order.getMedicineId());
    if (medicine.getStock() < order.getQuantity()) {
        return Result.error("库存不足");
    }
    medicine.setStock(medicine.getStock() - order.getQuantity());
    medicineMapper.updateById(medicine);  // 扣减库存
    order.setTotalPrice(medicine.getPrice().multiply(...));
    order.setStatus("UNPAID");
    orderMapper.insert(order);
    return Result.success();
}
```

支付时校验余额并扣款：

```java
@PostMapping("/orders/{id}/pay")
public Result<?> payOrder(@PathVariable Long id) {
    Order order = orderMapper.selectById(id);
    User user = userService.getById(userId);
    if (user.getBalance().compareTo(order.getTotalPrice()) < 0) {
        return Result.error("余额不足");
    }
    user.setBalance(user.getBalance().subtract(order.getTotalPrice()));
    userService.updateById(user);
    order.setStatus("PAID");
    orderMapper.updateById(order);
    return Result.success();
}
```

#### 4.2.5 软删除机制

为防止管理端删除数据影响医生端统计数据，预约记录和就诊记录采用软删除：

```java
@DeleteMapping("/appointments/{id}")
public Result<?> deleteAppointment(@PathVariable Long id) {
    Appointment appointment = appointmentMapper.selectById(id);
    appointment.setDeleted(1);      // 标记为已删除，不真删
    appointmentMapper.updateById(appointment);
    return Result.success();
}
```

查询时过滤已删除记录：

```java
@GetMapping("/appointments")
public Result<List<Appointment>> listAppointments() {
    return Result.success(appointmentMapper.selectList(
        new LambdaQueryWrapper<Appointment>()
            .eq(Appointment::getDoctorId, doctorId)
            .ne(Appointment::getDeleted, 1)));   // 排除已删除
}
```

统计接口包含已删除记录，保证医生端首页数据只增不减：

```java
@GetMapping("/consultations/stats")
public Result<Map<String, Long>> consultationStats() {
    List<Consultation> all = consultationMapper.selectList(
        new LambdaQueryWrapper<Consultation>().eq(Consultation::getDoctorId, doctorId));
    // all 包含 deleted=1 的记录，实现累加统计
    long total = all.size();
    long completed = all.stream().filter(c -> "COMPLETED".equals(c.getStatus())).count();
    return Result.success(Map.of("total", total, "completed", completed));
}
```

### 4.3 前端核心实现

#### 4.3.1 项目结构

```
vue-admin/src/
├── main.js                  # 入口：注册 Element Plus、Router、Pinia
├── App.vue                  # 根组件
├── api/index.js             # Axios 封装
├── router/index.js          # 路由配置 + 导航守卫
├── store/index.js           # Pinia 状态管理
└── views/
    ├── login/               # 登录/注册页
    ├── user/                # 用户端 10 个页面
    ├── doctor/              # 医生端 6 个页面
    └── admin/               # 管理端 10 个页面
```

#### 4.3.2 路由与权限控制

前端路由配置每个角色独立的路径前缀和布局组件：

```javascript
{ path: '/user', component: Layout, meta: { role: 'USER' },
  children: [
    { path: 'dashboard', component: Dashboard },
    { path: 'appointments', component: Appointments },
    // ...
  ]
}
```

导航守卫在页面跳转前检查 Token 和角色：

```javascript
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  const role = localStorage.getItem('role')
  if (to.path !== '/login' && !token) {
    next('/login')                        // 未登录 → 跳登录页
  } else if (to.meta.role && to.meta.role !== role) {
    next('/login')                        // 角色不匹配 → 跳登录页
  } else {
    next()                                // 放行
  }
})
```

#### 4.3.3 Axios 请求封装

统一的请求拦截器和响应拦截器：

```javascript
// 请求拦截器：自动注入 Token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截器：401 自动跳登录
request.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      localStorage.clear()
      router.push('/login')
    }
    return Promise.reject(error)
  }
)
```

#### 4.3.4 关键页面交互

**医生端就诊管理页面**：创建就诊记录时可以选择已通过的预约，自动填充宠物名和用户信息；编辑时不可修改关联的预约；只有"就诊中"状态的记录才显示"完成"按钮，点击后弹出二次确认。

**管理端列表页面**：已删除的记录在状态列显示红色"已删除"标签，且不显示删除按钮，防止重复操作。

**用户端订单管理页面**：根据订单状态动态显示操作按钮——未支付显示"支付"，已支付显示"收货"，已发货显示"收货"。

---

## 第5章 系统测试

### 5.1 测试环境

| 项目 | 配置 |
|------|------|
| 后端服务 | localhost:8080 |
| 前端服务 | localhost:3000 |
| 数据库 | MySQL 8.0 (pet_hospital) |
| 浏览器 | Chrome |

### 5.2 功能测试

#### 5.2.1 用户端功能测试

| 测试项 | 测试步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| 用户注册 | 填写注册信息，提交 | 注册成功，跳转登录页 | 通过 |
| 用户登录 | 输入用户名密码，选择"用户" | 登录成功，跳转用户首页 | 通过 |
| 预约挂号 | 选择医生，填写宠物信息 | 预约创建成功，状态为待审核 | 通过 |
| 修改预约 | 编辑预约信息，保存 | 保存成功，列表刷新 | 通过 |
| 删除预约 | 点击删除，确认 | 删除成功 | 通过 |
| 购买药品 | 选择药品，输入数量，确认 | 订单创建，库存扣减 | 通过 |
| 支付订单 | 点击支付，确认 | 余额扣减，状态变已支付 | 通过 |
| 确认收货 | 点击收货，确认 | 状态变已收货 | 通过 |
| 充值 | 输入金额，确认 | 余额增加 | 通过 |
| 查看就诊记录 | 进入就诊记录页 | 显示诊断和处方 | 通过 |
| 修改密码 | 输入旧密码和新密码 | 密码修改成功 | 通过 |

#### 5.2.2 医生端功能测试

| 测试项 | 测试步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| 审核通过 | 点击"通过"按钮 | 预约状态变已通过 | 通过 |
| 审核拒绝 | 点击"拒绝"按钮 | 预约状态变已拒绝 | 通过 |
| 创建就诊记录 | 选择预约，填写诊断处方 | 就诊记录创建，状态为就诊中 | 通过 |
| 完成就诊 | 点击"完成"按钮，确认 | 就诊和预约状态均变已完成 | 通过 |
| 查看统计数据 | 进入工作台 | 待处理/就诊记录/已完成正确显示 | 通过 |

#### 5.2.3 管理端功能测试

| 测试项 | 测试步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| 添加用户 | 填写用户信息，提交 | 用户创建成功 | 通过 |
| 编辑用户（不改密码） | 编辑其他信息，密码留空 | 保存成功，密码不变 | 通过 |
| 删除预约记录 | 点击删除，确认 | 状态变已删除，医生端统计不变 | 通过 |
| 删除就诊记录 | 点击删除，确认 | 状态变已删除，医生端统计不变 | 通过 |
| 订单发货 | 对已支付订单点发货 | 状态变已发货 | 通过 |

### 5.3 安全测试

| 测试项 | 测试步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| 未登录访问 | 直接访问 /user/dashboard | 跳转登录页 | 通过 |
| 角色越权 | USER 角色访问 /admin/users | 返回 403 | 通过 |
| 数据越权 | 用户 A 修改用户 B 的预约 | 返回无权限 | 通过 |
| 绕过支付 | 直接调 PUT /api/orders 改状态 | 只有未支付订单可修改 | 通过 |

### 5.4 测试结论

经过功能测试和安全测试，系统各模块运行正常，三种角色的权限隔离有效，核心业务流程（预约→审核→就诊→完成、下单→支付→发货→收货）流转正确，数据一致性得到保障。

---

## 第6章 总结与展望

### 6.1 工作总结

本课题完成了宠物医院后台管理系统的设计与实现，主要工作包括：

1. **需求分析**：调研宠物医院日常管理流程，梳理三种角色的功能需求
2. **系统设计**：完成数据库设计（6张表）、RESTful API 设计（40+个接口）、前后端架构设计
3. **系统实现**：后端使用 Spring Boot + MyBatis-Plus，完成业务逻辑、JWT 认证、角色权限控制；前端使用 Vue 3 + Element Plus，完成 26 个页面的开发
4. **系统测试**：完成功能测试和安全测试，验证系统正确性和安全性

系统的创新点：
- 软删除机制保证统计数据一致性
- 三层权限控制（拦截器路径校验 + 路由守卫 + 数据归属权校验）
- 预约→就诊→完成的状态联动

### 6.2 存在的不足

1. 密码加密采用 MD5，可升级为 BCrypt 等更安全的算法
2. 未实现数据分页，数据量大时性能会受影响
3. 缺少搜索和筛选功能，数据查找不够便捷
4. 未实现图片上传功能，无法存储宠物照片或检查报告
5. 缺少操作日志，管理员无法追溯操作历史
6. 界面未做响应式适配，移动端体验不佳

### 6.3 未来展望

1. 引入 Redis 缓存热点数据，提升查询性能
2. 接入微信小程序，方便宠物主人随时预约
3. 增加消息通知功能（预约审核结果、订单状态变更等）
4. 引入数据可视化图表，展示医院运营数据趋势
5. 添加操作日志和审计功能，满足合规要求

---

## 参考文献

[1] 李刚. Spring Boot 实战派[M]. 电子工业出版社, 2020.
[2] 尤雨溪. Vue.js 官方文档[EB/OL]. https://cn.vuejs.org/.
[3] MyBatis-Plus 团队. MyBatis-Plus 官方文档[EB/OL]. https://baomidou.com/.
[4] 刘增辉. MySQL 8.0 从入门到精通[M]. 清华大学出版社, 2020.
[5] Jones M, Bradley J, Sakimura N. JSON Web Token (JWT) [S]. RFC 7519, 2015.
[6] Element Plus 团队. Element Plus 官方文档[EB/OL]. https://element-plus.org/.
