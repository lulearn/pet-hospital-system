# 宠物医院后台管理系统的设计与实现

## 摘要

随着社会经济的发展和人们生活水平的提高，宠物已逐渐成为许多家庭的重要成员，宠物医疗服务的需求也随之快速增长。然而，当前许多中小型宠物医院仍采用传统的人工管理方式，面临着预约流程混乱、就诊记录难以追溯、药品库存管理粗放、数据统计困难等问题，严重制约了服务质量和运营效率的提升。

针对上述问题，本课题设计并实现了一套宠物医院后台管理系统。系统面向三种用户角色：宠物主人（用户）、医生和管理员，分别提供不同的功能模块。用户端支持在线浏览医生信息、预约挂号、宠物档案管理、购买药品、处方购药、订单支付与收货、账户充值、就诊记录查看与评价、消息通知等功能；医生端支持审核预约、就诊管理（诊断、处方、药品）、排班管理、完成就诊标记等功能；管理端支持用户管理、医生管理、药品管理、订单管理、预约与就诊记录管理、收入统计和数据导出等功能。

系统采用前后端分离的 B/S 架构。后端基于 Java 语言，使用 Spring Boot 2.7 框架搭建，通过 MyBatis-Plus 操作 MySQL 数据库，采用 JWT（JSON Web Token）实现无状态身份认证。前端使用 Vue 3 框架和 Element Plus 组件库，通过 Axios 与后端进行数据交互。系统在安全方面实现了三层权限控制——前端路由守卫、后端接口级角色校验以及数据归属权校验——有效保障了数据安全和访问控制。此外，系统引入了软删除机制，在管理端删除预约和就诊记录时仅标记而不真删，确保医生端统计数据的一致性和可追溯性。

本报告详细阐述了该系统的需求分析、系统设计、系统实现和系统测试的全过程，展示了完整的软件开发流程。

**关键词**：宠物医院；后台管理系统；Spring Boot；Vue 3；前后端分离；JWT

---

## 第1章 绪论

### 1.1 研究背景

#### 1.1.1 宠物行业发展趋势

近年来，随着我国经济的持续发展和城市化进程的加速，人民生活水平显著提高，饲养宠物的家庭数量不断增长。根据相关行业报告，中国宠物市场规模已突破数千亿元人民币，且保持着年均两位数的增长率。宠物在家庭中的角色也从传统的"看家护院"功能逐步转变为情感陪伴的"家庭成员"，这种转变直接推动了宠物医疗、宠物食品、宠物用品等产业链的蓬勃发展。

宠物医疗作为宠物产业链中的关键环节，其重要性不言而喻。宠物的健康问题直接关系到宠物主人的情感体验和经济支出，因此宠物主人对医疗服务的质量、便利性和透明度提出了越来越高的要求。

#### 1.1.2 传统管理模式的局限

尽管宠物医疗市场需求旺盛，但许多中小型宠物医院在日常管理方面仍然存在明显的短板，主要体现在以下几个方面：

第一，**预约管理效率低下**。大多数小型宠物医院的预约仍然依赖电话沟通或到店登记，前台人员需要手工在纸质登记本或简单的电子表格中记录预约信息。这种方式不仅容易出错（如时间冲突、漏记或重复预约），而且缺乏有效的提醒机制，导致医生无法提前了解当日的预约情况，也使得宠物主人无法实时查看预约状态。

第二，**就诊记录管理困难**。宠物缺乏类似人类的统一电子病历系统，每次就诊的诊断结果、处方和医嘱等信息通常以纸质形式保存。纸质病历容易丢失、受潮或字迹模糊，且不同医生之间的信息难以共享。当宠物主人更换医院或同一宠物再次就诊时，医生难以快速获取完整的历史就诊信息，影响诊断的准确性和连续性。

第三，**药品库存管理粗放**。部分宠物医院的药品库存管理依赖手工记账，难以及时掌握库存变化。一方面，可能出现热门药品缺货导致用户无法及时用药的情况；另一方面，也可能因为进货过多而造成药品积压以至过期浪费。此外，手工管理难以生成准确的库存报表，医院管理者无法有效分析药品的销售情况和制定采购计划。

第四，**订单与支付流程脱节**。在传统的管理模式下，宠物主人购买药品往往需要先到前台开单、再到药房取药、最后再回到前台缴费，流程繁琐且耗时。支付环节与订单记录分离，容易出现对账困难、漏收或重复收费等问题。此外，缺乏系统的支付记录也不利于医院的财务管理。

第五，**数据统计与分析缺失**。医院管理者难以获取全面、实时的运营数据，如各医生的接诊量、药品的销售情况、营业收入的趋势等。缺乏数据支撑导致管理决策主要依赖经验和直觉，不利于医院的长期发展和精细化管理。

综上所述，传统的宠物医院管理模式已无法满足行业发展的需求，亟需一套信息化管理系统来提升管理效率和服务质量。

### 1.2 研究目的与意义

#### 1.2.1 研究目的

本课题的研究目的是设计并实现一套适用于中小型宠物医院的后台管理系统，具体目标包括：

1. 实现宠物主人的在线服务功能，包括注册登录、医生浏览、预约挂号、药品购买、订单支付、账户充值和就诊记录查看等，使用户无需到店即可完成预约和购药操作。
2. 实现医生的在线工作功能，包括查看和处理预约、创建和填写就诊记录、录入诊断结果和处方等，使医生能够高效地管理自己的工作流程。
3. 实现管理员的全局管理功能，包括用户管理、医生管理、药品管理、订单管理和就诊记录管理等，使管理员能够全面掌控医院的运营情况。
4. 构建安全可靠的认证和权限控制体系，确保不同角色的用户只能访问和操作其权限范围内的数据和功能。
5. 引入合理的数据保护机制，如软删除和统计数据分离，确保关键数据的完整性和可追溯性。

#### 1.2.2 理论意义

从理论层面来看，本课题的研究意义主要体现在以下几个方面。首先，本系统采用了前后端分离的架构设计思想，这是当前 Web 应用开发的主流范式。通过将前端展示层和后端业务逻辑层分离，不仅提高了开发效率，也增强了系统的可维护性和可扩展性。本课题的实践过程和经验总结对于类似的毕业设计项目具有一定的参考价值。

其次，本系统在权限控制方面实现了多层次的安全防护——包括前端路由级权限控制、后端接口级角色校验以及数据归属权级校验——形成了一套较为完整的安全防护体系。这种多层防护的设计思路可以推广到其他需要角色管理的系统中。

再次，本系统引入的软删除机制是数据库设计中的一种常见实践，但本课题将其与统计数据的一致性需求进行了有机结合——既满足管理端数据清理的需求，又保证了业务端统计数据的完整性——这种设计思路对类似场景具有借鉴意义。

#### 1.2.3 实践意义

从实践层面来看，本课题具有以下现实意义：

第一，提升医院运营效率。系统将预约、就诊、购药等业务环节从线下转移到线上，减少了人工操作环节，降低了出错概率。医生可以通过系统快速了解每日预约情况，管理员可以实时掌握药品库存和订单状态，整体运营效率得到显著提升。

第二，改善用户体验。宠物主人可以随时随地通过系统浏览医生信息、预约挂号、购买药品和查看就诊记录，无需多次往返医院，大大节省了时间成本。同时，系统的充值功能和在线支付功能使得交易过程更加便捷和透明。

第三，保障数据安全和可追溯性。系统采用软删除机制和统计分离设计，确保就诊记录和预约记录的统计数据不会被误删操作影响。医生可以回看自己所有的历史就诊记录，管理员可以管理所有数据但不会破坏医生端的统计。

第四，为医院管理决策提供数据支撑。系统为管理员提供了全局数据视图，可以实时了解用户数量、医生工作负载、药品销售情况和订单收入等关键指标，为医院的管理决策提供数据依据。

### 1.3 国内外研究现状

#### 1.3.1 国外研究现状

在欧美发达国家，兽医诊所的信息化建设起步较早，发展相对成熟。早在 21 世纪初，就有商业化的兽医诊所管理系统（Veterinary Practice Management System, VPMS）投入市场。目前比较成熟的商业产品包括 eVetPractice、Vetter Software、AVImark 等。

这些系统通常集成了电子病历（EMR）、预约排班、库存管理、账单管理、客户关系管理（CRM）等功能模块，部分系统还支持在线预约、远程问诊和移动端访问。例如，eVetPractice 提供了基于云的 SaaS 解决方案，兽医可以通过网页或平板设备随时访问系统；Vetter Software 则专注于无纸化诊所管理，支持条形码扫描药品管理和自动提醒功能。

在实际功能层面，国外系统通常具有以下特点：一是与实验室设备和影像设备集成，可以自动导入检查结果；二是支持宠物主人通过客户门户在线查看宠物的病历、预约和账单信息；三是提供强大的报表和数据分析功能，帮助诊所管理者了解业务趋势。

在学术研究方面，国外学者对兽医诊所信息化的研究主要集中在以下几个方面：电子病历的标准化和互操作性、基于人工智能的辅助诊断系统、宠物健康数据的大数据分析等。

#### 1.3.2 国内研究现状

相比国外，国内宠物医疗行业的信息化建设起步较晚。长期以来，国内宠物医疗行业以小型诊所为主，经营者对信息化的重视程度和投入意愿较低。直到近年来，随着宠物经济的快速发展和连锁宠物医院的兴起，信息化管理系统才逐渐受到关注。

当前国内市场上的宠物医院管理系统大致可分为两类。第一类是面向大型连锁宠物医院的定制化系统，这些系统通常由专业软件公司开发，功能全面但价格昂贵，且需要专业的 IT 运维支持，对于小型医院来说成本过高。第二类是针对中小型医院的通用管理软件，多为单机版或简单的 B/S 架构系统，功能相对单一，通常只覆盖了挂号和收费等基础功能。

在学术研究方面，国内高校近年来也有一些关于宠物医院管理系统的学位论文和相关研究，技术路线涵盖 Java Web、PHP、.NET 等。这些研究在功能设计上各有侧重，但多数在安全性设计、数据保护和多角色协同方面仍存在不足。

#### 1.3.3 现有系统的不足

综合来看，现有系统主要存在以下不足：

1. **成本门槛高**：商业系统价格昂贵，小型医院难以负担。
2. **功能过度或不足**：大型系统功能冗余，小型系统功能单一，难以匹配中小型医院的实际需求。
3. **安全性设计不足**：部分系统对角色权限的控制较为薄弱，存在数据越权访问的风险。
4. **数据保护机制缺失**：多数系统缺少软删除和数据保护机制，误删操作可能导致重要数据永久丢失。
5. **多角色协同不完善**：用户、医生、管理员之间的业务流程衔接不够顺畅，特别是预约→就诊→完成的状态流转缺乏自动化处理。

本课题的研究正是针对以上不足，设计一套功能适中、安全可靠、适合中小型宠物医院使用的后台管理系统。

### 1.4 主要研究内容

本课题的主要研究内容包括以下几个方面：

1. **需求分析**：调研宠物医院的日常管理流程，梳理三种用户角色的功能需求，确定系统的功能边界和非功能需求。
2. **系统设计**：完成系统的架构设计、数据库设计、API 接口设计，确立系统的整体技术方案。
3. **后端开发**：基于 Spring Boot 框架，使用 MyBatis-Plus 完成数据持久层开发，使用 JWT 实现认证和授权，实现三个端点的全部业务接口。
4. **前端开发**：基于 Vue 3 框架和 Element Plus 组件库，使用 Vue Router 和 Pinia 实现路由管理和状态管理，完成二十余个前端页面的开发。
5. **安全机制实现**：实现多层权限控制、密码加密存储、软删除数据保护等安全机制。
6. **系统测试**：完成功能测试和安全测试，验证系统的正确性和安全性。

### 1.5 论文结构

本论文共分为六章。

第一章为绪论，介绍了课题的研究背景、研究目的与意义、国内外研究现状以及主要研究内容。

第二章为需求分析，详细描述了系统的功能需求和非功能需求，分析了三种角色的业务流程和用例场景。

第三章为系统设计，详细阐述了系统的架构设计、技术选型、数据库设计和 API 接口设计。

第四章为系统实现，从后端和前端两个方面详细描述了系统各模块的具体实现过程。

第五章为系统测试，描述了测试环境、测试方法、测试用例和测试结果。

第六章为总结与展望，对全文工作进行总结，指出存在的不足和改进方向。

---

## 第2章 需求分析

### 2.1 系统总体需求概述

宠物医院后台管理系统是一套面向中小型宠物医院的信息化管理平台，旨在为宠物主人、医生和管理员提供统一的工作平台。系统需要支持三种用户角色，每种角色的功能需求有明确的边界。

系统的核心业务流程为：宠物主人通过系统注册账号，浏览医生信息并进行预约挂号；医生收到预约后，对预约进行审核（通过或拒绝），审核通过后可为该宠物主人创建就诊记录，填写诊断结果和处方，并在就诊完成后标记完成；同时，宠物主人可以在线浏览药品信息并下单购买，支付后等待管理员发货，收到药品后确认收货。管理员则负责全局的用户管理、医生管理、药品管理和订单管理。

#### 2.1.1 用户角色定义

系统定义了三种用户角色，每种角色的身份和职责如下。

**用户（宠物主人）**：系统的核心服务对象。用户通过注册创建账号，登录后可以进行以下操作：浏览医院的所有医生信息，根据医生的擅长领域和个人简介选择合适的医生进行预约挂号；在"我的预约"页面中管理自己的预约记录，包括新增、修改和删除预约；浏览医院的所有药品信息，包括药品名称、描述、单价和库存数量，根据需求选择药品并下单购买；在"我的订单"页面中管理自己的订单，包括支付未付款订单和确认已收到药品；在"我的就诊记录"页面中查看医生为自己宠物开具的诊断结果和处方；在"充值中心"页面为自己的账户充值；在"个人中心"页面修改自己的个人信息和登录密码。

**医生**：系统的主要服务提供者。医生通过注册或由管理员添加创建账号，登录后可以进行以下操作：在工作台首页查看待处理的预约数量、就诊记录总数和已完成数量；在"预约管理"页面查看分配给自己的所有预约记录，对状态为"待审核"的预约进行通过或拒绝操作，对已有预约进行编辑修改；在"就诊管理"页面查看自己的所有就诊记录，创建新的就诊记录（选择已通过的预约、填写宠物名称、诊断结果、处方和备注），编辑已有的就诊记录，对"就诊中"的就诊记录标记完成（点击完成后，就诊记录和关联的预约记录同时变更为已完成状态）。

**管理员**：系统的全局管理者。系统预设一个管理员账号（用户名 admin，初始密码 admin123），管理员登录后可以进行以下操作：在工作台首页查看系统的关键统计数据，包括用户数量、医生数量、药品数量和订单数量；在"用户管理"页面查看所有用户信息，添加新用户，编辑已有用户信息，删除用户；在"医生管理"页面查看所有医生信息，添加新医生，编辑医生信息，删除医生；在"预约记录"页面查看系统所有预约记录，对已完成的旧记录进行软删除（标记删除，不真删数据）；在"药品管理"页面维护药品信息，包括新增药品、修改药品名称/描述/价格/库存、删除药品；在"订单管理"页面查看所有订单，对已支付的订单进行发货操作，删除未支付或已收货的订单；在"就诊记录"页面查看所有就诊记录，对旧记录进行软删除；在"个人中心"页面修改个人信息和密码。

### 2.2 功能性需求分析

#### 2.2.1 用户端功能需求详述

**(1) 用户注册与登录**

宠物主人首次使用系统时需要进行注册。注册时需填写以下信息：用户名（用于登录系统的唯一标识）、密码（登录凭据）、真实姓名、手机号码、电子邮箱。提交注册后，系统对用户名进行唯一性校验，如果用户名已被占用则提示用户更换。注册成功后，密码以 MD5 加密形式存入数据库，系统返回注册成功提示并引导用户前往登录页面。

登录时，用户需要输入用户名和密码，并选择"用户"角色。系统根据用户名和角色查询数据库，将用户输入的密码进行 MD5 加密后与数据库中存储的密文进行比对。比对成功则生成 JWT Token 返回给前端，前端将 Token 和用户信息存储在 localStorage 中，并跳转至用户端首页。比对失败则返回"用户名或密码错误"的提示。

**(2) 首页工作台**

用户登录后首先进入首页工作台，页面以三个统计卡片的形式展示用户的关键数据：我的预约数（所有状态的预约总数）、我的订单数（所有状态的订单总数）、当前账户余额。这些数据通过调用三个后端接口（获取我的预约列表、获取我的订单列表、获取个人信息）来获取。

**(3) 医生浏览与预约**

用户可以在"就诊预约"页面浏览医院所有医生的信息。每位医生以卡片形式展示，包括医生姓名、擅长领域、个人简介。用户点击"预约"按钮后，系统弹出预约对话框。

预约对话框中需要填写以下信息：宠物名称（必填）、宠物种类（选填，如猫、狗、兔子等）、病情描述（选填，用于提前告知医生宠物的基本情况）、预约时间（通过日期时间选择器选择）。系统自动将当前登录用户的 ID 和在浏览中选中的医生 ID 绑定到预约记录中。提交后预约创建成功，状态默认为"待审核"（PENDING），等待该医生的审核。

**(4) 我的预约管理**

用户可以在"预约管理"页面查看和管理自己的所有预约记录。页面以表格形式展示，每条记录包含以下信息：预约 ID、宠物名称、宠物种类、病情描述、预约时间、当前状态（以不同颜色的标签展示：待审核为橙色、已通过为绿色、已拒绝为红色、就诊中为蓝色、已完成为绿色）、操作按钮。

操作按钮根据预约状态动态显示。对于所有状态的预约，均显示"编辑"按钮，点击可修改宠物名称、种类、描述和预约时间。对于"待审核"状态的预约，额外显示"删除"按钮，点击并确认后可删除该预约。已通过或就诊中的预约不可删除。

**(5) 药品浏览与购买**

用户可以在"药品管理"页面浏览医院提供的所有药品。页面以表格形式展示，每条药品记录包含药品名称、描述、单价、库存数量。用户可以浏览药品信息，了解每种药品的用途和价格。

用户点击某条药品记录的"购买"按钮后，系统弹出购买对话框。用户输入购买数量并确认后，系统进行以下处理：首先校验购买数量是否大于 0 且不超过当前库存数量，如果超出库存则提示"库存不足"；校验通过后，计算订单总价（单价 × 数量），扣减药品库存，创建订单记录（状态为"未支付"），并自动刷新药品列表以反映最新的库存数量。整个流程在同一个后端接口调用中完成，确保了数据的一致性。

**(6) 我的订单管理**

用户可以在"订单管理"页面查看和管理自己的所有订单记录。页面以表格形式展示，每条记录包含以下信息：药品名称（通过药品 ID 查询并映射为名称显示）、购买数量、订单总价、订单状态（以不同颜色的标签展示：未支付为灰色、已支付为蓝色、已发货为橙色、已收货为绿色）、创建时间、操作按钮。

操作按钮根据订单状态动态变化。对于"未支付"状态的订单，显示"支付"和"删除"按钮。点击"支付"按钮并确认后，系统校验用户余额是否充足，充足则从余额中扣除订单金额，订单状态变为"已支付"，支付时间记录为当前时间。对于"已支付"或"已发货"状态的订单，显示"收货"按钮，点击确认后订单状态变为"已收货"，收货时间记录为当前时间。对于"已收货"状态的订单，仅显示"删除"按钮。

每个状态的操作按钮设计确保了订单流程的单向性——用户不能跳过支付直接收货，也不能在收货后退回已支付状态。

**(7) 就诊记录查看**

用户可以在"就诊记录"页面查看医生为自己开具的所有就诊记录。页面以表格形式展示，每条记录包含以下信息：医生姓名（通过医生 ID 映射为姓名显示）、宠物名称、诊断结果、处方内容、医生备注、就诊时间。用户只能查看自己的就诊记录，无法修改或删除。

**(8) 充值中心**

用户可以在此页面为自己的账户充值。页面顶部显示当前余额，下方提供金额输入框和充值按钮。用户输入充值金额并点击确认后，弹出二次确认对话框（"确认充值 X 元？"），确认后系统将充值金额累加到用户的账户余额中。

**(9) 个人中心与密码修改**

用户可以在"个人中心"页面查看和修改个人信息。用户名字段为禁用状态，不可修改；可修改的字段包括真实姓名、手机号码和电子邮箱。页面底部设有"修改密码"按钮，点击后跳转到独立的密码修改页面。

密码修改页面要求用户输入旧密码、新密码和确认新密码（两次输入新密码以便确认）。系统会进行以下校验：新密码与确认新密码是否一致；旧密码是否正确（系统将输入的旧密码进行 MD5 加密后与数据库中存储的密码密文进行比对）。校验通过后，将新密码进行 MD5 加密并更新到数据库。整个操作需要用户进行二次确认（弹窗提示"确认修改密码？"）后才能完成。

#### 2.2.2 医生端功能需求详述

**(1) 医生登录**

医生通过登录页面输入用户名和密码，选择"医生"角色进行登录。系统根据医生角色到医生表中查询，密码比对成功则生成 JWT Token 返回给前端，前端存储 Token 并跳转至医生端首页。

**(2) 工作台统计**

医生登录后首先进入工作台首页。页面以三个统计卡片展示医生的关键工作数据：待处理预约数（当前需要审核的 PENDING 状态预约数量）、就诊记录数（该医生的所有就诊记录总数，包括软删除的，确保只增不减）、已完成数（该医生已完成的预约总数，包括软删除的，确保只增不减）。

这些统计数据通过两个特殊的统计接口（`/api/doctor/appointments/stats` 和 `/api/doctor/consultations/stats`）获取，而非直接从页面列表计算。这种设计使得统计数据与管理端的数据删除操作解耦——即使管理端软删除了某些记录，医生端的统计数据仍然保持不变，始终反映该医生的历史工作总量。

**(3) 预约审核与管理**

医生可以在"预约管理"页面查看分配给自己的所有预约记录。页面以表格形式展示，每条记录包含以下信息：用户名称（通过用户 ID 映射为姓名显示，不再显示 ID 数字）、宠物名称、宠物种类、病情描述、预约时间、当前状态（以不同颜色的标签展示）、操作按钮。

医生对预约的操作有三种。"通过"按钮仅在预约状态为"待审核"时显示，点击后状态变为"已通过"。"拒绝"按钮同样仅在"待审核"状态下显示，点击后状态变为"已拒绝"。"编辑"按钮对所有预约均显示，点击弹出编辑对话框，可以修改宠物名称、宠物种类、预约时间和病情描述。

之前医生端预约管理页面还存在"删除"按钮，后续优化中去掉了该按钮。因为预约记录是医院的重要业务数据，医生不应随意删除。如果需要清理过期记录，由管理端进行软删除操作。

**(4) 就诊管理**

医生可以在"就诊管理"页面查看和管理自己的所有就诊记录。页面以表格形式展示，每条记录包含以下信息：用户姓名、宠物名称、诊断结果、处方内容、状态（就诊中或已完成，以标签展示）、就诊时间、操作按钮。

医生点击"添加就诊记录"按钮，弹出就诊记录创建对话框。对话框中，医生首先从下拉列表中选择一条"已通过"状态的预约（列表显示宠物名称和用户姓名，方便医生识别）。选中预约后，系统自动填充宠物名称、用户 ID 和关联的预约 ID。医生需要手动填写诊断结果（textarea，多行文本，描述宠物的病症和诊断结论）、处方（textarea，多行文本，记录开具的药品和用法用量）、备注（textarea，选填，记录其他需要说明的事项）。提交后，就诊记录创建成功，状态为"就诊中"，同时关联的预约记录状态同步变更为"就诊中"。

在就诊记录表格中，每条记录的操作列显示"编辑"按钮。对于状态为"就诊中"的记录，还额外显示"完成"按钮。医生点击"完成"按钮并确认后，就诊记录状态变更为"已完成"，同时关联的预约记录状态也同步变更为"已完成"。这个联动机制确保了预约和就诊的状态一致性。"已完成"的就诊记录不再显示"完成"按钮，医生只能查看和编辑其内容。

之前医生端就诊管理页面同样存在"删除"按钮，后续优化中已去掉。就诊记录作为医疗档案，不应被随意删除。

#### 2.2.3 管理端功能需求详述

**(1) 管理员登录**

系统预设的管理员账号通过登录页面登录。管理员在登录页面输入用户名和密码，前端检测到用户名为"admin"时，自动将角色识别为"ADMIN"并调用对应的登录接口。登录成功后跳转至管理端首页。

**(2) 工作台统计**

管理端首页以四个统计卡片展示系统的全局数据：用户总数、医生总数、药品总数、订单总数。这些数据通过调用对应的后端列表接口获取（取返回数组的长度作为计数）。

**(3) 用户管理**

管理员可以在此页面查看系统中所有注册用户的列表，以表格形式展示。每条记录包含用户 ID、用户名、真实姓名、手机号码、角色（USER 或 ADMIN）和账户余额。

管理员可以添加新用户。点击"添加用户"按钮弹出对话框，需要填写用户名、密码、真实姓名、手机号码、邮箱，并通过下拉选择框指定角色（用户或管理员）。提交后，后端直接创建用户并自动对密码进行 MD5 加密存储，不再走注册流程（注册流程会强制设定角色为 USER）。这意味着管理员可以通过该系统创建管理员账号。

管理员可以编辑已有用户信息。点击"编辑"按钮弹出预填当前信息的对话框。管理员可以修改除用户 ID 和用户名以外的其他字段。密码字段留空表示不修改原密码；如果填写了新密码，后端会对新密码进行 MD5 加密后更新。这一设计解决了早期版本中编辑用户会导致密码被清空的 Bug——之前前端会将空字符串作为密码发送到后端，MyBatis-Plus 将其更新到数据库，导致用户无法登录。现在的修复方案是：前端判断密码为空则删除 password 字段，后端判断密码为空或空字符串则将该字段设为 null（MyBatis-Plus 跳过 null 字段的更新）。

管理员可以删除用户。点击"删除"按钮并确认后，该用户被从数据库中彻底删除。

**(4) 医生管理**

该页面的功能和操作逻辑与用户管理类似。管理员可以查看所有医生的列表（含用户名、姓名、擅长领域、手机号码），添加新医生（需填写用户名、密码、姓名、手机号码、邮箱、擅长领域、个人简介），编辑医生信息（密码留空不修改，填写则加密更新），删除医生。与早期版本不同的是，当前版本在添加和编辑医生时都会对密码进行 MD5 加密处理，解决了之前医生密码以明文存储导致无法登录的问题。

**(5) 预约记录管理**

管理员可以在此页面查看系统中所有的预约记录。每条记录以表格行展示，包含以下信息：用户姓名（通过用户 ID 映射）、医生姓名（通过医生 ID 映射）、宠物名称、宠物种类、病情描述、预约时间、状态标签、删除标记状态。

状态标签不仅展示预约自身的状态（待审核/已通过/已拒绝/就诊中/已完成），还可以显示软删除标记——如果某条记录已被软删除，状态列会显示红色"已删除"标签。

管理员对预约的操作仅为软删除。对于未被软删除的记录，操作列显示"删除"按钮。点击并确认后，该记录的 deleted 字段被设为 1，数据行本身保留在数据库中。已软删除的记录不显示删除按钮，防止重复操作。

软删除是区别于物理删除（真删）的数据保护机制。预约记录是医院的核心业务数据，物理删除会导致以下问题：一是医生端的已完成统计数据会减少，影响医生工作量的准确评估；二是如果未来需要追溯历史预约记录进行核对或审计，物理删除的数据无法恢复。软删除通过在表中增加一个 deleted 标记字段（TINYINT 类型，0 表示正常，1 表示已删除），在保留数据的同时实现"删除"的效果。

与软删除机制配套的是数据的查询过滤和统计分离。医生端和用户端的预约列表查询时会自动过滤掉 deleted=1 的记录（通过 `.ne(Appointment::getDeleted, 1)` 条件），因此用户和医生看到的列表是干净的。但医生端首页的统计接口使用的是包含软删除记录的全量数据，确保"已完成"计数只增不减。

**(6) 药品管理**

管理员可以在此页面对药品信息进行完整的增删改查操作。药品列表以表格形式展示，包含药品名称、药品描述、单价和库存数量。添加药品时需要填写名称、描述、单价和库存数量。编辑药品时可以修改所有字段。删除药品会从数据库中彻底删除该药品记录。

**(7) 订单管理**

管理员可以在此页面查看系统中所有的订单记录。每条订单以表格行展示，包含以下信息：用户姓名（通过用户 ID 映射）、药品名称（通过药品 ID 映射）、购买数量、订单总价、订单状态（以标签展示，包括未支付/已支付/已发货/已收货）、创建时间。

管理员对订单有两种操作。第一是"发货"操作——仅对状态为"已支付"的订单显示。管理员点击发货按钮后，订单状态变更为"已发货"，发货时间记录为当前时间。这个操作表示管理员已根据订单信息向用户发出了药品。第二是"删除"操作——仅对状态为"未支付"或"已收货"的订单显示。未支付的订单说明用户已放弃购买，已收货的订单说明交易已完结，这两种状态的订单可以被清理。正在进行中的订单（已支付、已发货）不可删除，以保障交易的完整性。

**(8) 就诊记录管理**

管理员可以在此页面查看系统中所有的就诊记录。每条记录以表格行展示，包含以下信息：用户姓名、医生姓名、宠物名称、诊断结果、处方内容、状态（就诊中/已完成/已删除）、就诊时间。

与预约记录一样，就诊记录也采用软删除机制。管理员点击"删除"按钮后，记录的 deleted 字段被设为 1，数据行保留。已软删除的就诊记录在医生端和用户端的列表页不可见，但医生的就诊统计数据保持包含。所有就诊记录页面中，已软删除的记录在状态列显示红色"已删除"标签。

**(9) 个人中心与密码修改**

管理员的个人中心功能与用户端类似。管理员可以查看和修改自己的个人信息（用户名不可修改），可以通过独立的密码修改页面（需要输入旧密码和两次新密码，并进行二次确认）来更改登录密码。

#### 2.2.4 新增功能需求详述

在完成基础功能后，通过对系统的全面审视，补充了以下9项增强功能，使系统更加完善和实用。

**(1) 忘记密码功能**

用户在登录页面点击"忘记密码"链接，跳转至重置密码页面。该页面要求用户输入用户名、手机号码、新密码和确认密码。系统根据用户名分别在用户表和医生表中查询，如果找到匹配且手机号一致，则将新密码进行 MD5 加密后更新。这一功能解决了用户遗忘密码后无法登录的问题，且不需要管理员的介入。

**(2) 宠物档案管理**

用户可以在"宠物档案"页面管理自己的宠物信息，包括宠物名称、种类、品种、年龄和性别。页面以表格展示用户的所有宠物，支持增删改查操作。预约时，用户可以从已有的宠物档案中选择宠物，系统自动填充宠物名称和种类信息，既方便了用户操作，也确保了预约信息的准确性。宠物档案同样采用软删除机制，删除后将deleted字段标记为1。

**(3) 医生排班管理**

医生可以在"排班管理"页面设置自己每周的出诊时间。每条排班记录包含星期（周一到周日）、开始时间、结束时间和该时段的最大接诊数量。用户在进行预约前，可以查看医生的排班表，了解医生的出诊时间，从而合理选择就诊时段。医生可以随时添加和删除排班记录，已删除的排班记录通过软删除机制标记。

**(4) ECharts 数据可视化看板**

系统在三个端的首页引入了 ECharts 图表，替代了原本单一的统计卡片展示。用户端首页展示预约状态分布的饼图和订单状态的柱状图，帮助用户直观了解自己的预约和消费情况。医生端首页展示预约状态分布的环形图和评分分布的柱状图，帮助医生了解工作负载和服务评价。管理端首页展示订单收入统计饼图和平台数据概览柱状图，并在页面底部提供数据导出操作区。此外，管理端首页还新增了"待处理预约"和"本月收入"两个统计卡片。

**(5) 就诊评价功能**

用户在就诊记录页面可以对已完成的就诊进行 1-5 星的评分。评分功能通过点击"评价"按钮弹出星级选择对话框实现。已评分的就诊记录在表格中直接显示星级（只读）。医生端首页通过柱状图展示评分分布，帮助医生了解自己的服务质量。后端在 rating 字段存储评分值，在 ratedAt 字段记录评价时间。只有状态为"已完成"的就诊才可以评价，且评价后不可修改。

**(6) 消息通知机制**

当预约状态发生变更时，系统自动向相关用户推送通知消息。触发通知的场景包括：医生通过预约（"您的预约已被医生通过，请按时到院就诊"）、医生拒绝预约（"您的预约已被医生拒绝，请重新预约"）、医生开始就诊（"您的宠物已在就诊中"）、医生完成就诊（"您的宠物就诊已完成，请对本次服务进行评价"）。用户端新增"消息通知"页面，以表格展示所有通知，未读通知以红色标签标记，支持单条标记已读和全部标记已读。首页统计卡片中显示未读通知数量，帮助用户及时了解消息动态。

**(7) 处方-购药联动**

医生在编辑就诊记录时，可以为其添加处方药品。医生从药品列表中选择药品，填写数量和用法用量（如"每日2次，每次1片"）。药品通过 prescription_medicine 表与就诊记录关联存储。用户在查看就诊记录时，可以点击"药方"按钮查看医生开具的药品明细，并可以直接点击"购买"按钮从处方一键创建药品订单，无需再到药品页面手动查找和购买。这一功能打通了就诊和购药的业务流程，提升了用户体验。

**(8) 收入统计模块**

管理端新增了两个统计接口。`/api/admin/stats/dashboard` 返回用户数、医生数、药品种类数、订单总数、待处理预约数和当月收入等关键运营指标。`/api/admin/stats/revenue` 返回总收入以及各状态订单的数量统计。前端管理端首页通过 6 个统计卡片展示这些数据，并结合 ECharts 饼图展示收入分布。

**(9) 数据导出功能**

管理端首页新增数据导出区域，支持将预约记录和订单记录导出为 CSV 文件。导出时，后端通过 JOIN 查询或 ID 映射，将用户 ID、医生 ID、药品 ID 等外键字段替换为对应的名称，生成可直接阅读和使用的数据。CSV 文件名包含导出日期，支持中文（通过 UTF-8 BOM 头确保 Excel 正确识别编码）。

### 2.3 非功能性需求分析

除了功能需求之外，系统还需要满足以下非功能性需求。

#### 2.3.1 安全性需求

系统处理的是宠物主人的个人信息、医生的诊断记录和医院的经营数据，安全性是系统设计的首要考虑因素之一。具体的安全需求包括：

1. **认证安全**：所有业务接口（除登录和注册外）必须进行认证检查，未携带有效 Token 的请求应被拒绝并返回 401 状态码。Token 应有合理的过期时间（本系统设置为 24 小时），过期后用户需要重新登录。
2. **密码安全**：用户和医生的密码在数据库中必须以密文形式存储，不能存储明文。本系统采用 MD5 摘要算法对密码进行加密。
3. **权限隔离**：三种角色的权限应有明确边界。普通用户不能访问医生端或管理端的接口；医生不能访问管理端的接口；管理员虽然权限最高，但在用户端和医生端的数据操作中也需要遵守业务规则。
4. **数据归属权**：用户只能查看和操作自己的预约、订单和就诊记录；医生只能处理分配给自己名下的预约和就诊记录。即使用户通过技术手段（如修改请求参数中的 ID）试图访问他人的数据，后端也应拦截并拒绝。

#### 2.3.2 可用性需求

系统的目标用户包括普通宠物主人，他们的计算机操作水平参差不齐，因此系统在可用性方面有以下要求：

1. **界面直观**：功能布局符合常见的管理系统习惯，左侧菜单分级清晰，表格操作直观，按钮含义明确。
2. **操作反馈**：每次操作后应有明确的结果提示（成功或失败），关键操作（如删除、支付、修改密码）应有二次确认弹窗，防止误操作。
3. **数据实时更新**：页面的数据应在操作后自动刷新，不需要用户手动刷新浏览器。例如购买药品后药品库存立即更新，支付订单后订单状态立即变化。
4. **状态可视化**：不同状态的预约、订单、就诊记录应使用不同颜色的标签进行区分，方便用户快速识别。

#### 2.3.3 可维护性需求

作为毕业设计项目，系统的代码应具备良好的可读性和可维护性：

1. **代码结构清晰**：后端按照 Controller → Service → Mapper 三层架构组织，前端按照 views → router → store → api 的组织方式分层。
2. **命名规范**：Java 类和方法遵循驼峰命名规则，数据库字段使用下划线命名并通过 MyBatis-Plus 自动映射。前端组件名和文件名保持一致。
3. **注释适当**：关键的业务逻辑、复杂的判断条件、非显而易见的处理流程应添加简洁的注释说明。

#### 2.3.4 性能需求

考虑到中小型宠物医院的业务规模（注册用户数百到数千，日均预约数十到数百条），系统在性能方面的要求相对宽松：

1. 页面首次加载时间应在 3 秒以内。
2. 普通列表查询的接口响应时间应在 1 秒以内。
3. 数据写入操作（创建预约、下单等）的响应时间应在 2 秒以内。
4. 系统应能支持 50 个并发用户同时使用。

### 2.4 用例分析

#### 2.4.1 用户端用例

用户（宠物主人）的主要用例包括：

- **注册账号**：用户打开注册页面，填写个人注册信息并提交。系统检查用户名是否已被占用，未被占用则创建账号，密码以 MD5 加密形式存储。
- **登录系统**：用户输入用户名和密码，选择"用户"角色。系统验证身份后返回 JWT Token，用户进入用户端工作台首页。
- **浏览医生并预约**：用户进入就诊预约页面，查看医生列表。选择合适的医生后，填写宠物信息、选择预约时间并提交。系统创建一条"待审核"状态的预约记录。
- **管理我的预约**：用户在预约管理页面查看所有预约。可编辑未完成的预约，可删除待审核状态的预约。
- **购买药品**：用户在药品管理页面浏览药品，选择需要的药品并输入数量。系统检查库存后计算金额、扣减库存、生成未支付订单。
- **支付订单**：用户在订单管理页面找到未支付订单，点击支付。系统检查余额并扣款，订单状态更新为"已支付"。
- **确认收货**：用户收到药品后在订单管理页面点击收货。订单状态更新为"已收货"。
- **查看就诊记录**：用户进入就诊记录页面，查看医生开具的所有诊断记录和处方。
- **账户充值**：用户在充值中心输入金额并确认。系统累加到用户的账户余额。
- **修改个人信息**：用户在个人中心页面修改姓名、手机号、邮箱等信息。
- **修改密码**：用户在密码修改页面输入旧密码和新密码。系统验证旧密码后更新为新密码。
- **忘记密码**：用户在登录页点击"忘记密码"，输入用户名、手机号和新密码。系统验证身份后重置密码。
- **管理宠物档案**：用户在宠物档案页面添加、编辑和删除宠物信息。预约时从档案中选择宠物。
- **评价就诊**：用户对已完成的就诊进行 1-5 星评分。
- **查看药方并购药**：用户在就诊记录中查看处方药品明细，从处方直接一键购买。
- **查看通知**：用户查看系统推送的消息通知，标记已读。

#### 2.4.2 医生端用例

医生的主要用例包括：

- **登录系统**：医生选择"医生"角色，输入用户名和密码登录。系统验证后跳转医生端工作台。
- **查看工作台统计**：医生登录后查看待处理预约数、就诊记录总数和已完成数量。
- **审核预约**：医生在预约管理页面查看待审核的预约。根据宠物主人的描述和自己的时间安排，决定通过或拒绝该预约。
- **编辑预约**：医生可以编辑分配给自己的预约信息，如修改宠物名称、调整预约时间等。
- **创建就诊记录**：医生在就诊管理页面选择一条已通过的预约，填写诊断结果、处方和备注，创建就诊记录。创建后预约状态同步变更为"就诊中"。
- **编辑就诊记录**：医生可编辑已有的就诊记录，修改诊断结果、处方等内容。
- **完成就诊**：当宠物诊疗结束后，医生点击"完成"按钮。就诊记录和关联的预约记录状态同步变更为"已完成"。
- **管理排班**：医生设置每周的出诊时间（星期、时间段、最大接诊数）。
- **管理药方**：医生在就诊记录中添加处方药品，指定数量和用法用量。

#### 2.4.3 管理端用例

管理员的主要用例包括：

- **登录系统**：管理员用系统预设账号（或后续创建的管理员账号）登录。
- **管理用户**：查看所有用户，添加新用户，编辑用户信息，删除用户。
- **管理医生**：查看所有医生，添加新医生，编辑医生信息，删除医生。
- **管理药品**：查看所有药品，添加新药品，编辑药品信息，删除药品。
- **管理订单**：查看所有订单，对已支付订单执行发货，删除未支付或已收货的订单。
- **管理预约记录**：查看所有预约记录，对旧记录执行软删除。
- **管理就诊记录**：查看所有就诊记录，对旧记录执行软删除。
- **查看收入统计**：在工作台首页查看总收入、各状态订单数和当月收入等运营数据。
- **导出数据**：将预约记录和订单记录导出为 CSV 文件，便于离线分析和存档。

### 2.5 可行性分析

#### 2.5.1 技术可行性

本系统采用的技术栈均为当前业界成熟且广泛使用的开源技术。Spring Boot 是 Java 生态中最流行的微服务开发框架，具有丰富的文档和社区支持。Vue 3 是前端三大主流框架之一，配合 Element Plus 组件库可以快速构建美观的管理界面。MySQL 是世界上最流行的开源关系型数据库。这些技术在本科阶段的教学中有所涉及，开发难度适中，技术上是完全可行的。

#### 2.5.2 经济可行性

系统开发使用的所有技术和工具均为免费和开源的，包括 JDK、Maven、MySQL、Node.js、Vue 3、Element Plus 等，不需要任何商业授权费用。开发环境使用个人电脑，部署到服务器也只需常规的云服务器或本地服务器，经济成本极低。因此，经济上是完全可行的。

#### 2.5.3 操作可行性

系统采用 B/S 架构，用户通过浏览器即可访问，不需要安装任何客户端软件。操作界面采用常见的管理系统布局（左侧菜单 + 顶部导航栏 + 中间内容区），用户学习成本低。关键操作设有二次确认机制，降低了误操作的风险。因此，操作上是完全可行的。

---

## 第3章 系统设计

### 3.1 系统总体架构设计

本系统采用 B/S（Browser/Server，浏览器/服务器）架构，遵循前后端分离的设计原则。系统整体分为三个层次：前端表现层、后端业务逻辑层和数据持久层。各层之间通过标准化的接口进行通信，层次之间高内聚、低耦合，便于开发、测试和维护。

#### 3.1.1 架构层次说明

**前端表现层**运行在用户的浏览器中，使用 Vue 3 框架构建。这一层负责页面的渲染和用户交互，包括路由跳转、表单验证、数据展示等。前端通过 Axios 库向后端发送 HTTP 请求（GET、POST、PUT、DELETE），获取 JSON 格式的数据或提交用户的操作请求。前端不直接操作数据库，所有数据读写均通过后端接口完成。

**后端业务逻辑层**运行在服务器上，使用 Spring Boot 框架构建。这一层接收前端发来的 HTTP 请求，经过 JWT 认证拦截器校验身份和权限后，由 Controller 层接收请求参数，调用 Service 层处理业务逻辑，再通过 Mapper 层与数据库交互。处理完成后，将结果封装为统一的 JSON 格式返回给前端。

业务逻辑层内部又可分为三个子层：Controller 层（控制器层，负责接收请求和返回响应）、Service 层（服务层，负责业务逻辑处理，如密码加密、库存扣减、余额检查等）和 Mapper 层（数据访问层，负责与数据库的交互，由 MyBatis-Plus 的 BaseMapper 提供基础的 CRUD 能力）。

**数据持久层**采用 MySQL 8.0 关系型数据库，用于持久化存储系统的所有业务数据。数据库中的 10 张核心表分别对应用户、医生、宠物档案、药品、预约、就诊记录、订单、医生排班、消息通知和处方药品数据。

#### 3.1.2 前后端交互流程

一次典型的请求-响应流程如下：

1. 用户在前端页面进行操作（如点击"提交预约"按钮）。
2. 前端的 Axios 请求拦截器自动从 localStorage 中取出 JWT Token，设置到请求的 Authorization Header 中（格式为 `Bearer <token>`）。
3. 前端通过 Axios 发送 HTTP 请求到后端对应的 URL（如 `POST /api/appointments`）。
4. 后端的 JWT 拦截器拦截该请求，从 Header 中提取 Token，解析并校验其有效性。无效则返回 401，有效则继续。
5. 拦截器检查请求路径是否与 Token 中的角色匹配（如 `/api/admin/` 路径需要 ADMIN 角色）。不匹配则返回 403。
6. 校验通过后，拦截器将 Token 中提取的 userId、username、role 存入 request 对象的属性中，供后续 Controller 使用。
7. 请求到达 Controller 对应的方法。Controller 从 request 中获取当前用户 ID，从请求体中解析 JSON 参数。
8. Controller 调用 Service 层进行业务处理（如校验预约归属权、校验库存、扣减余额等）。
9. Service 层通过 Mapper 层读写数据库。
10. Controller 将处理结果封装为统一的 Result 对象（包含 code、message 和 data 三个字段），以 JSON 格式返回给前端。
11. 前端的 Axios 响应拦截器接收响应。如果响应码是 401，自动清除 Token 并跳转登录页；否则将数据传递给前端组件。
12. 前端组件根据返回的数据更新页面展示（如刷新表格、更新状态标签等）。

### 3.2 技术选型

#### 3.2.1 后端技术选型

**Java 语言与 Spring Boot 框架**：Java 是最成熟的企业级开发语言之一，具有跨平台、稳定性好、生态丰富等优势。Spring Boot 是 Spring 生态中的快速开发框架，通过自动配置和内嵌的 Tomcat 服务器大幅简化了项目的搭建和部署工作。开发者无需配置复杂的 XML 文件，仅需少量的注解和 YAML 配置即可启动一个完整的 Web 服务。选择 Spring Boot 的另一个原因是它在 Java Web 教学中的普遍性，便于在毕业设计答辩中进行技术讲解。

**MyBatis-Plus ORM 框架**：MyBatis-Plus 是对 MyBatis 的增强，在保持 MyBatis 原有功能的基础上提供了更多便捷特性。它内置了通用 Mapper（BaseMapper），使开发者无需手写基本的增删改查 SQL 语句。例如，`userMapper.selectById(1)` 会自动生成 `SELECT * FROM user WHERE id = 1` 并执行。它还提供了 LambdaQueryWrapper，支持使用 Java 的 Lambda 表达式构建类型安全的查询条件，避免了字符串拼接带来的字段名拼写错误风险。本项目中的所有查询都使用了 LambdaQueryWrapper，没有编写任何 XML 映射文件。

**MySQL 数据库**：MySQL 是全球最流行的开源关系型数据库，具有性能高、成本低、社区支持好等优势。本系统选择 MySQL 8.0 版本，该版本在性能、安全性和 SQL 标准支持方面较早期版本有显著提升。

**JWT 认证方案**：JWT（JSON Web Token）是一种无状态的认证方案。相比传统的 Session + Cookie 方案，JWT 不需要在服务器端维护会话状态，这使得系统更易于横向扩展。每个 JWT 是一个自包含的加密字符串，包含了用户身份信息（如 userId、username、role）和过期时间。本系统使用 jjwt 库生成和解析 Token，签名算法采用 HS256。

#### 3.2.2 前端技术选型

**Vue 3 框架**：Vue.js 是目前最流行的前端框架之一，以其轻量、易上手和渐进式设计著称。Vue 3 引入了 Composition API（组合式 API），使用 `ref()` 和 `reactive()` 函数实现响应式数据绑定，使得组件的逻辑可以更灵活地组织和复用。本项目中的全部组件均使用 `<script setup>` 语法糖编写，代码更加简洁。

**Element Plus 组件库**：Element Plus 是 Vue 3 生态中使用最广泛的桌面端 UI 组件库。它提供了丰富的预构建组件，如表单（el-form）、表格（el-table）、对话框（el-dialog）、标签（el-tag）、按钮（el-button）、菜单（el-menu）等。使用组件库的好处是开发者无需从零编写 HTML 和 CSS，直接使用组件就可以快速构建出风格统一、交互规范的界面。

**Vue Router 路由**：Vue Router 是 Vue.js 的官方路由管理器，用于实现单页面应用（SPA）中不同页面的切换。本项目使用嵌套路由定义每种角色下的页面层级结构，并在导航守卫中实现角色权限的前端校验。

**Pinia 状态管理**：Pinia 是 Vue 3 官方推荐的状态管理库，替代了旧版的 Vuex。本项目使用 Pinia 管理全局的用户登录状态（Token、用户信息、角色），在登录后将信息持久化到 localStorage 中，在退出登录时清空。

**Axios 网络请求库**：Axios 是目前最流行的基于 Promise 的 HTTP 客户端。本项目使用 Axios 封装了统一的请求和响应拦截器，实现了 Token 自动注入、401 统一处理等功能。

**Vite 构建工具**：Vite 是 Vue 3 的官方推荐构建工具，以其极速的冷启动和热模块替换（HMR）显著提升了开发体验。

### 3.3 功能模块设计

系统按角色划分为三大功能模块组，每个模块组包含若干子模块。以下为新增功能在各端的模块归属。

#### 3.3.1 用户端模块划分

用户端包含以下子模块：认证模块（登录、注册和忘记密码）、首页工作台模块（含 ECharts 图表和未读消息统计）、医生浏览与预约模块（含宠物选择和排班查看）、预约管理模块、宠物档案管理模块、药品浏览与购买模块、订单管理模块、就诊记录查看模块（含评价和药方购药）、消息通知模块、充值中心模块、个人中心模块（含密码修改）。

#### 3.3.2 医生端模块划分

医生端包含以下子模块：认证模块（登录）、工作台统计模块（含 ECharts 图表和评分分布）、预约审核与管理模块、就诊管理模块（含药方管理）、排班管理模块、个人中心模块（含密码修改）。

#### 3.3.3 管理端模块划分

管理端包含以下子模块：认证模块（登录）、工作台统计模块（含 ECharts 图表、收入统计和数据导出）、用户管理模块、医生管理模块、预约记录管理模块（软删除）、药品管理模块、订单管理模块（发货和删除）、就诊记录管理模块（软删除）、个人中心模块（含密码修改）。

### 3.4 数据库设计

#### 3.4.1 数据库概述

本系统使用 MySQL 8.0 关系型数据库，数据库名为 `pet_hospital`，默认字符集为 utf8mb4。系统共设计了 10 张核心业务表，分别用于存储用户信息、医生信息、宠物档案、药品信息、预约记录、就诊记录、订单记录、医生排班、消息通知和处方药品关联数据。

#### 3.4.2 数据库表结构详细设计

**(1) 用户表（user）**

用户表用于存储系统中的所有用户账号信息。用户和部分管理员共用此表，通过 role 字段区分角色。用户和医生采用分表设计（用户表和医生表分开），是因为两者虽然都有登录功能，但数据结构存在较大差异：医生有擅长领域、个人简介等专有字段，而用户有账户余额等专用字段。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 用户唯一标识 |
| username | VARCHAR | 50 | 否 | — | — | 登录用户名 |
| password | VARCHAR | 100 | 否 | — | — | 登录密码，MD5 加密存储 |
| real_name | VARCHAR | 50 | 是 | — | — | 用户真实姓名 |
| phone | VARCHAR | 20 | 是 | — | — | 手机号码 |
| email | VARCHAR | 50 | 是 | — | — | 电子邮箱 |
| role | VARCHAR | 20 | 是 | USER | — | 角色：USER 或 ADMIN |
| balance | DECIMAL | 10,2 | 是 | 0 | — | 账户余额 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 注册时间 |

核心设计说明：password 字段存储的是 MD5 加密后的密文，不是明文密码。即使用户表数据泄露，攻击者也无法直接获取用户的原始密码。balance 字段采用 DECIMAL(10,2) 类型而非 FLOAT，以避免浮点数计算中的精度问题（这在涉及金额的场景中非常重要）。

**(2) 医生表（doctor）**

医生表用于存储所有医生的账号和简介信息。医生表与用户表在结构上分开，但在认证逻辑上有相似之处——两者都支持独立的登录流程。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 医生唯一标识 |
| username | VARCHAR | 50 | 否 | — | — | 登录用户名 |
| password | VARCHAR | 100 | 否 | — | — | 登录密码，MD5 加密 |
| real_name | VARCHAR | 50 | 是 | — | — | 医生真实姓名 |
| phone | VARCHAR | 20 | 是 | — | — | 手机号码 |
| email | VARCHAR | 50 | 是 | — | — | 电子邮箱 |
| specialty | VARCHAR | 100 | 是 | — | — | 擅长领域，如"犬科"、"猫科" |
| description | TEXT | — | 是 | — | — | 个人简介，展示给用户看 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 注册时间 |

核心设计说明：specialty 字段用于在用户浏览医生时展示医生的专长，方便宠物主人根据宠物的种类和病症选择合适的医生。description 字段使用 TEXT 类型（而非 VARCHAR），因为它可能包含较长的个人介绍文本。与用户表类似，password 字段同样以 MD5 密文形式存储。

**(3) 药品表（medicine）**

药品表用于存储医院提供的药品信息，是用户购买药品和管理员管理库存的基础数据。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 药品唯一标识 |
| name | VARCHAR | 100 | 否 | — | — | 药品名称 |
| description | TEXT | — | 是 | — | — | 药品描述（功效、用法等） |
| price | DECIMAL | 10,2 | 是 | — | — | 药品单价 |
| stock | INT | — | 是 | 0 | — | 当前库存数量 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 录入时间 |

核心设计说明：stock 库存字段使用 INT 类型存储整数。在订单创建时，系统会检查 stock 是否足够并实时扣减。price 与 balance 一样使用 DECIMAL(10,2) 保证金额精度。库存扣减操作在创建订单的同一个事务中完成，保证了数据一致性。

**(4) 预约表（appointment）**

预约表是系统的核心业务表之一，记录了宠物主人的预约信息以及预约的处理状态。该表通过 user_id 和 doctor_id 分别与用户表和医生表关联。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 预约唯一标识 |
| user_id | BIGINT | — | 否 | — | 外键 | 关联用户 ID |
| doctor_id | BIGINT | — | 否 | — | 外键 | 关联医生 ID |
| pet_name | VARCHAR | 50 | 否 | — | — | 宠物名称 |
| species | VARCHAR | 50 | 是 | — | — | 宠物种类（猫、狗、兔等） |
| description | TEXT | — | 是 | — | — | 病情描述 |
| status | VARCHAR | 20 | 是 | PENDING | — | 预约状态 |
| deleted | TINYINT | — | 是 | 0 | — | 软删除标记 |
| appointment_time | DATETIME | — | 是 | — | — | 预约的就诊时间 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 预约创建时间 |

status 字段的取值及其含义：
- PENDING：待审核（用户刚提交预约，等待医生处理）
- APPROVED：已通过（医生审核通过，等待就诊）
- REJECTED：已拒绝（医生审核拒绝）
- IN_PROGRESS：就诊中（医生创建了就诊记录，正在诊治）
- COMPLETED：已完成（医生完成就诊，本次服务结束）

deleted 字段用于软删除机制。0 表示正常记录，1 表示已被管理端软删除。查询时通过条件过滤 `deleted != 1` 来排除已删除记录，而统计接口则包含所有记录（含 deleted=1 的），确保统计数据只增不减。

**(5) 就诊记录表（consultation）**

就诊记录表用于存储医生为每个病例开具的诊断结果和处方，是医疗档案的核心数据表。该表通过 doctor_id、user_id 和 appointment_id 分别与医生表、用户表和预约表关联。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 就诊记录唯一标识 |
| doctor_id | BIGINT | — | 否 | — | 外键 | 就诊医生 ID |
| user_id | BIGINT | — | 否 | — | 外键 | 就诊用户 ID |
| appointment_id | BIGINT | — | 是 | — | 外键 | 关联的预约 ID |
| pet_name | VARCHAR | 50 | 是 | — | — | 宠物名称 |
| diagnosis | TEXT | — | 是 | — | — | 诊断结果 |
| prescription | TEXT | — | 是 | — | — | 处方内容 |
| notes | TEXT | — | 是 | — | — | 备注 |
| status | VARCHAR | 20 | 是 | IN_PROGRESS | — | 就诊状态 |
| deleted | TINYINT | — | 是 | 0 | — | 软删除标记 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 就诊时间 |

status 字段取值及含义：
- IN_PROGRESS：就诊中（医生已创建就诊记录，正在诊治过程中）
- COMPLETED：已完成（医生完成诊治并点击完成按钮）

deleted 字段的作用与预约表相同，支持管理端的软删除操作。

核心设计说明：就诊记录与预约记录通过 appointment_id 关联。一条预约最多对应一条就诊记录（1:1 关系）。当医生创建就诊记录时，关联的预约状态由 APPROVED 变为 IN_PROGRESS。当医生完成就诊时，就诊记录和关联的预约记录同时变为 COMPLETED。这种状态联动设计确保了数据的一致性。

诊断结果和处方使用 TEXT 类型而非 VARCHAR，是因为这些内容通常是多行的长文本，VARCHAR 的长度限制（MySQL 中最大 65535 字符）虽然对于大多数场景足够，但 TEXT 类型更灵活。备注字段同样使用 TEXT 类型，用于存储医生认为需要额外说明的信息。

**(6) 订单表（orders）**

订单表用于存储用户购买药品的订单信息，记录了从下单、支付、发货到收货的完整流程数据。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 订单唯一标识 |
| user_id | BIGINT | — | 否 | — | 外键 | 下单用户 ID |
| medicine_id | BIGINT | — | 否 | — | 外键 | 药品 ID |
| quantity | INT | — | 否 | — | — | 购买数量 |
| total_price | DECIMAL | 10,2 | 是 | — | — | 订单总价 |
| status | VARCHAR | 20 | 是 | UNPAID | — | 订单状态 |
| pay_time | DATETIME | — | 是 | — | — | 支付时间 |
| ship_time | DATETIME | — | 是 | — | — | 发货时间 |
| receive_time | DATETIME | — | 是 | — | — | 收货时间 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 下单时间 |

status 字段取值及含义：
- UNPAID：未支付（订单刚创建，等待用户付款）
- PAID：已支付（用户已完成付款，等待管理员发货）
- SHIPPED：已发货（管理员已发货，等待用户收货）
- RECEIVED：已收货（用户已确认收到药品，交易完成）

核心设计说明：订单的状态流转是单向的，依次为 UNPAID → PAID → SHIPPED → RECEIVED，不允许跳步或回退。pay_time、ship_time、receive_time 三个时间字段分别记录各状态变更的时间点，为后续的数据分析和审计提供依据。total_price 在订单创建时计算（单价 × 数量），后续不可修改，以确保订单金额的确定性。

**(7) 宠物档案表（pet）**

宠物档案表用于存储用户管理的宠物信息，支持用户在预约时从已有宠物中选择。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 宠物唯一标识 |
| user_id | BIGINT | — | 否 | — | 外键 | 所属用户 ID |
| name | VARCHAR | 50 | 否 | — | — | 宠物名称 |
| species | VARCHAR | 50 | 是 | — | — | 种类（狗、猫、兔等） |
| breed | VARCHAR | 50 | 是 | — | — | 品种（金毛、英短等） |
| age | INT | — | 是 | — | — | 年龄 |
| gender | VARCHAR | 10 | 是 | — | — | 性别（公/母） |
| deleted | TINYINT | — | 是 | 0 | — | 软删除标记 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 创建时间 |

核心设计说明：每只宠物通过 user_id 关联到具体的用户，用户只能管理自己的宠物。同样采用软删除机制。

**(8) 医生排班表（doctor_schedule）**

医生排班表用于存储医生设置的工作时间安排，用户在预约时可查看。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 排班唯一标识 |
| doctor_id | BIGINT | — | 否 | — | 外键 | 医生 ID |
| day_of_week | INT | — | 否 | — | — | 星期（1=周一，7=周日） |
| start_time | TIME | — | 否 | — | — | 出诊开始时间 |
| end_time | TIME | — | 否 | — | — | 出诊结束时间 |
| max_appointments | INT | — | 是 | 5 | — | 该时段最大接诊数 |
| deleted | TINYINT | — | 是 | 0 | — | 软删除标记 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 创建时间 |

核心设计说明：day_of_week 采用整数表示星期（1-7），便于程序进行日期计算。start_time 和 end_time 使用 TIME 类型存储。max_appointments 用于限制该时段的预约数量。

**(9) 消息通知表（notification）**

消息通知表用于存储系统自动推送的用户通知，如预约状态变更等。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 通知唯一标识 |
| user_id | BIGINT | — | 否 | — | 外键 | 接收用户 ID |
| title | VARCHAR | 100 | 否 | — | — | 通知标题 |
| content | TEXT | — | 是 | — | — | 通知内容 |
| is_read | TINYINT | — | 是 | 0 | — | 是否已读 |
| create_time | DATETIME | — | 是 | CURRENT_TIMESTAMP | — | 发送时间 |

核心设计说明：is_read 字段用于区分未读和已读通知。通知由系统在关键业务操作时自动创建（如预约状态变更、就诊完成等），不需要用户手动操作。

**(10) 处方药品表（prescription_medicine）**

处方药品表用于存储就诊记录中医生开具的药品信息，是就诊记录和药品之间的关联表。

| 字段名 | 数据类型 | 长度 | 是否允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|-----------|--------|------|------|
| id | BIGINT | — | 否 | 自增 | 主键 | 记录唯一标识 |
| consultation_id | BIGINT | — | 否 | — | 外键 | 就诊记录 ID |
| medicine_id | BIGINT | — | 否 | — | 外键 | 药品 ID |
| quantity | INT | — | 是 | 1 | — | 数量 |
| dosage | VARCHAR | 100 | 是 | — | — | 用法用量 |

核心设计说明：一条就诊记录可以关联多条处方药品（一对多），每种药品都有独立的数量和用法用量信息。用户可以从处方药品直接下单购买。

#### 3.4.3 实体关系分析

系统 10 张表之间的实体关系可以总结如下：

用户表（user）与预约表（appointment）是一对多关系：一个用户（宠物主人）可以创建多条预约记录，通过 appointment 表的 user_id 外键关联。

医生表（doctor）与预约表（appointment）是一对多关系：一个医生可以被多个用户预约，通过 appointment 表的 doctor_id 外键关联。

医生表（doctor）与就诊记录表（consultation）是一对多关系：一个医生可以创建多条就诊记录，通过 consultation 表的 doctor_id 外键关联。

用户表（user）与就诊记录表（consultation）是一对多关系：一个用户（宠物主人）可以有多条就诊记录（对应不同时间或不同宠物），通过 consultation 表的 user_id 外键关联。

预约表（appointment）与就诊记录表（consultation）是一对一关系：一条预约（被医生审核通过后）最多对应一条就诊记录，通过 consultation 表的 appointment_id 外键关联。

用户表（user）与订单表（orders）是一对多关系：一个用户可以创建多条订单，通过 orders 表的 user_id 外键关联。

药品表（medicine）与订单表（orders）是一对多关系：一种药品可以被多个用户下单购买，通过 orders 表的 medicine_id 外键关联。

用户表（user）与宠物档案表（pet）是一对多关系：一个用户可以管理多只宠物的档案，通过 pet 表的 user_id 外键关联。

医生表（doctor）与排班表（doctor_schedule）是一对多关系：一个医生可以设置多条排班记录，通过 doctor_schedule 表的 doctor_id 外键关联。

用户表（user）与消息通知表（notification）是一对多关系：一个用户可以收到多条通知，通过 notification 表的 user_id 外键关联。

就诊记录表（consultation）与处方药品表（prescription_medicine）是一对多关系：一条就诊记录可以包含多种处方药品，通过 prescription_medicine 表的 consultation_id 外键关联。

药品表（medicine）与处方药品表（prescription_medicine）是一对多关系：一种药品可以出现在多条处方中，通过 prescription_medicine 表的 medicine_id 外键关联。

#### 3.4.4 数据库优化考虑

对于毕业设计的数据规模，当前的 10 张表设计已经满足需求。但从长期的优化角度考虑，以下措施可以在数据量增大后采取：

1. 在 user_id、doctor_id、status 等高频查询字段上创建索引，加速查询。
2. 对 appointment 和 orders 等数据量增长较快的表，可以考虑按时间进行分区（如按月分区）。
3. 对于医生列表、药品列表等读取频率远大于写入频率的数据，可以引入 Redis 缓存来减少数据库压力。

### 3.5 API 接口设计

系统的所有 API 接口遵循 RESTful 设计风格，以 `/api` 为统一前缀，使用标准 HTTP 方法表示操作类型：GET 表示查询，POST 表示新增，PUT 表示修改，DELETE 表示删除。

接口返回统一的 JSON 格式：

```json
{
  "code": 200,        // 状态码，200 表示成功，其他值表示失败
  "message": "操作成功", // 提示信息
  "data": {}           // 返回的具体数据
}
```

#### 3.5.1 认证接口

认证接口是系统中仅有的两个不需要携带 Token 的接口。

登录接口（POST /api/login）：接收三个参数——username（用户名）、password（密码）、role（角色）。后端根据 role 参数判断到哪个表中进行查询：role 为 USER 或 ADMIN 时查询 user 表，role 为 DOCTOR 时查询 doctor 表。查询到匹配的用户/医生后，将输入的密码进行 MD5 加密并与数据库中的密文比对。比对成功则生成包含 userId、username、role 三个 claims 的 JWT Token（有效期 24 小时），将 Token 和用户信息一起返回。比对失败则返回错误码和提示信息。

注册接口（POST /api/register）：接收注册所需的基本信息。后端首先检查用户名是否已被占用，已占用则提示更换。未占用则创建新记录，密码进行 MD5 加密后存储，角色根据注册时用户的选择来设定（USER 或 DOCTOR）。

重置密码接口（POST /api/reset-password）：接收用户名、手机号和新密码。后端先在医生表中核验用户名和手机号的匹配关系，再在用户表中核验。匹配成功则将密码进行 MD5 加密后更新。这一设计确保了只有账号持有者才能重置密码。

#### 3.5.2 用户端接口

用户端的接口主要分为以下几组：

医生信息接口（GET /api/doctors）：返回系统中所有医生的列表，每条记录包含医生的基本信息（姓名、擅长领域、简介等）。该接口不需要权限校验，任何登录用户都可以查看。

预约管理接口：提供预约的增删改查功能。GET /api/appointments 获取当前用户的预约列表（通过 Token 中的 userId 过滤，只返回当前用户的预约）。POST /api/appointments 创建新预约（自动绑定当前用户 ID，状态默认为 PENDING）。PUT /api/appointments 修改预约（先校验预约是否属于当前用户，不属于则拒绝）。DELETE /api/appointments/{id} 删除指定预约（同样需要归属权校验）。

药品接口（GET /api/medicines）：返回系统中所有药品的列表，包括药品名称、描述、单价和库存数量。用户端只能查看药品，无法修改或删除药品信息。

订单管理接口：GET /api/orders 获取当前用户的订单列表。POST /api/orders 创建订单（包含药品 ID 和购买数量），后端处理时进行库存校验和扣减、金额计算。DELETE /api/orders/{id} 删除订单（归属权校验）。POST /api/orders/{id}/pay 支付订单（归属权校验 + 余额校验 + 扣款）。POST /api/orders/{id}/receive 确认收货（归属权校验）。

就诊记录接口（GET /api/consultations）：返回当前用户的就诊记录列表（通过 Token 中的 userId 过滤），用户端为只读，不能修改或删除就诊记录。

充值接口（POST /api/recharge）：接收充值金额，将金额累加到当前用户的 balance 字段。

个人中心接口：GET /api/user/profile 获取当前用户的个人信息。PUT /api/user/profile 修改个人信息（后端会忽略 role 和 balance 字段的修改，防止用户自行提升权限或修改余额）。

密码修改接口（PUT /api/user/change-password）：接收旧密码和新密码。后端先验证旧密码是否正确，正确则用 MD5 加密新密码并更新。

宠物档案接口（GET/POST/PUT/DELETE /api/pets）：完整的 CRUD 操作，数据经 user_id 过滤，用户只能操作自己的宠物。删除采用软删除机制。

医生排班查看接口（GET /api/doctor-schedules/{doctorId}）：用户可查看指定医生的排班表，了解出诊时间。

就诊评价接口（PUT /api/consultations/{id}/rate）：用户对已完成的就诊打分，接收 1-5 的评分值。后端校验就诊归属权和完成状态后才允许评分。

处方药品查看接口（GET /api/consultations/{id}/medicines）：查看指定就诊记录的处方药品明细，包含药品 ID、数量、用法用量。

处方购药接口（POST /api/consultations/{id}/buy-medicine）：从处方药品直接购买。后端根据药品 ID 和数量校验库存并创建订单。

消息通知接口（GET /api/notifications）：获取当前用户的通知列表，按时间降序排列。PUT /api/notifications/{id}/read 标记单条已读。PUT /api/notifications/read-all 标记全部已读。GET /api/notifications/unread-count 获取未读通知数量。

#### 3.5.3 医生端接口

医生端的接口以 `/api/doctor` 为前缀，所有接口只返回与当前登录医生相关的数据。

预约管理接口：GET /api/doctor/appointments 获取分配给当前医生的预约列表（通过 doctor_id 过滤，且排除已软删除的记录）。PUT /api/doctor/appointments 修改预约（归属权校验 + 状态变更审核）。

预约统计接口（GET /api/doctor/appointments/stats）：返回当前医生的预约统计数据，包含 pending（待处理数，只计未删除的 PENDING 记录）和 completed（已完成数，包含已软删除的记录）。

就诊管理接口：GET /api/doctor/consultations 获取当前医生的就诊记录列表（通过 doctor_id 过滤，排除已软删除的记录）。POST /api/doctor/consultations 创建就诊记录（绑定当前医生 ID，状态默认为 IN_PROGRESS，同时将关联预约状态改为 IN_PROGRESS）。PUT /api/doctor/consultations 修改就诊记录（归属权校验）。PUT /api/doctor/consultations/{id}/complete 完成就诊（将就诊记录和关联预约的状态都变为 COMPLETED）。

就诊统计接口（GET /api/doctor/consultations/stats）：返回当前医生的就诊统计数据，total 为所有就诊记录总数（含已软删除），completed 为已完成的就诊记录数。

用户列表接口（GET /api/doctor/users）：返回所有用户的基本信息，供医生在预约管理页面将用户 ID 映射为用户姓名显示。

排班管理接口（GET/POST/DELETE /api/doctor/schedules）：医生管理自己的排班。GET 获取排班列表，POST 新增排班，DELETE 删除（软删除）。

处方药品管理接口（GET/POST/DELETE /api/doctor/consultations/{id}/medicines）：医生在就诊记录中管理处方药品。GET 获取已添加的药品列表，POST 添加药品及用法用量，DELETE 删除药品。

个人中心和密码修改接口的功能与用户端类似，但操作的是 doctor 表。

#### 3.5.4 管理端接口

管理端的接口以 `/api/admin` 为前缀，返回系统中的全局数据，不受所属关系限制。这些接口只有 ADMIN 角色的用户可以访问（由 JWT 拦截器强制校验）。

用户管理接口：GET /api/admin/users 返回所有用户的完整信息（包括密码密文——前端不显示密码字段）。POST /api/admin/users 创建用户（不走注册流程，保留管理员设定的角色，密码自动加密）。PUT /api/admin/users 编辑用户（密码为空不修改，有值则加密更新）。DELETE /api/admin/users/{id} 物理删除用户。

医生管理接口：GET /api/admin/doctors 返回所有医生信息。POST /api/admin/doctors 添加医生（密码自动加密）。PUT /api/admin/doctors 编辑医生（密码处理同上）。DELETE /api/admin/doctors/{id} 物理删除医生。

预约记录管理接口：GET /api/admin/appointments 返回所有预约记录（含已软删除的）。DELETE /api/admin/appointments/{id} 软删除指定预约（设置 deleted=1）。

药品管理接口：完整的 CRUD 接口，提供 GET/POST/PUT/DELETE 四种方法。

订单管理接口：GET /api/admin/orders 返回所有订单。PUT /api/admin/orders/{id}/ship 发货（只能对已支付订单操作）。DELETE /api/admin/orders/{id} 物理删除订单（只能删除未支付或已收货的订单）。

就诊记录管理接口：GET /api/admin/consultations 返回所有就诊记录（含已软删除的）。DELETE /api/admin/consultations/{id} 软删除指定就诊记录（设置 deleted=1）。

仪表盘统计接口（GET /api/admin/stats/dashboard）：返回系统的全局运营数据，包括用户数、医生数、药品数、订单数、待处理预约数和当月收入。

收入统计接口（GET /api/admin/stats/revenue）：返回总收入和各状态订单的数量统计（已支付、已发货、已收货数量）。

数据导出接口（GET /api/admin/export/appointments 和 GET /api/admin/export/orders）：将预约记录和订单记录导出为包含用户/医生/药品名称的结构化数据。前端负责将数据转换为 CSV 格式并提供下载。

### 3.6 安全设计

#### 3.6.1 认证安全

系统采用 JWT 作为认证方案。每个 Token 使用 HS256 算法进行签名，签名密钥配置在 application.yml 中（实际生产部署时应使用环境变量注入）。Token 中包含 userId、username 和 role 三个 claims，以及过期时间（当前设置为 24 小时）。

JwtInterceptor 拦截器注册在 Spring MVC 的拦截器链中，拦截路径为 `/api/**`，排除 `/api/login` 和 `/api/register`（这两个是公开接口）。拦截器在处理每个请求时，首先检查 HTTP 方法是否为 OPTIONS（浏览器跨域的预检请求），是则直接放行。然后从 Authorization Header 中提取 Token（格式为 `Bearer <token>`），去掉 "Bearer " 前缀后调用 JwtUtil 进行解析和校验。校验不通过（Token 过期、签名无效、Token 为空等）则返回 401 JSON 响应。

#### 3.6.2 权限控制

系统实现了三层权限控制机制。

第一层是前端路由守卫（Vue Router beforeEach）。在每次路由跳转前，前端从 localStorage 中读取 token 和 role。如果目标路径不是登录或注册页面且没有 token，则重定向到登录页。如果有 token，则检查目标路由的 meta.role 是否与当前 role 匹配——不匹配说明用户试图访问其他角色的页面，同样重定向到登录页。这层控制的主要作用是优化用户体验，避免用户看到不属于自己角色的页面空白或报错。

第二层是后端接口级权限校验（JWT 拦截器）。在 Token 解析通过后，拦截器根据请求的路径前缀判断权限。以 `/api/admin/` 开头的路径只允许 role 为 ADMIN 的用户访问，以 `/api/doctor/` 开头的路径只允许 role 为 DOCTOR 的用户访问。权限不足则返回 403 JSON 响应。这层控制是真正的安全防线，即使用户绕过前端直接调用后端接口，也会被拦截器拒绝。

第三层是数据归属权校验（Controller 层业务代码）。在用户端和医生端的具体业务接口中，Controller 会从 request 中取出当前用户的 ID，与操作目标数据的所有者 ID 进行比对。例如用户修改预约时，后端先查出该预约的 user_id，与当前用户的 ID 比对，不一致则返回"无权操作"。这层控制防止了同角色用户之间的数据越权。

#### 3.6.3 数据安全

密码在数据库中采用 MD5 摘要算法加密存储。注册、登录、管理端添加用户/医生等所有涉及密码的场景中，后端都会将密码进行 MD5 加密后再存储或比对。前端在传输密码时使用 HTTPS 保护传输层安全（在生产环境中应配置 SSL 证书）。

订单支付过程中的金额计算使用 DECIMAL 类型，避免浮点数精度问题导致的金额误差。订单状态变更（支付、发货、收货）的后端逻辑中进行了严格的权限和条件校验，防止用户通过修改请求参数绕过业务流程。

#### 3.6.4 软删除机制

软删除是本系统的一个重要数据保护机制。预约记录表和就诊记录表中设有 deleted 字段，当管理端执行"删除"操作时，并不实际删除数据行，而是将 deleted 字段设为 1。

这种设计配合两套查询逻辑：列表查询时过滤 deleted=1，确保日常使用的页面中不显示已删除的数据；统计查询时包含所有记录，确保医生端首页的"就诊记录总数"和"已完成数"只增不减。通过这种方式，既满足了管理端清理老旧数据的需求，又保护了医生端统计数据的完整性和可追溯性。

---

## 第4章 系统实现

### 4.1 开发环境

系统的开发和测试环境配置如下：

| 项目 | 配置 |
|------|------|
| 开发机器操作系统 | Ubuntu 22.04 / Windows 10 |
| JDK 版本 | 17.0.18 (OpenJDK) |
| 项目构建工具 | Maven 3.8 |
| 数据库 | MySQL 8.0.35 |
| 集成开发环境 | IntelliJ IDEA / VS Code |
| Node.js 版本 | 18.x |
| npm 版本 | 9.x |
| 浏览器 | Google Chrome |
| 版本控制 | Git |
| 代码托管平台 | GitHub |

### 4.2 后端项目搭建

#### 4.2.1 项目初始化与依赖配置

后端项目基于已有的 Spring Boot 骨架项目进行扩展。首先在 `pom.xml` 中添加项目所需的依赖。核心依赖包括以下几个。

Spring Boot Starter Web：这是 Spring Boot 的核心 Web 启动器，内置了 Tomcat 服务器和 Spring MVC，提供了 RESTful API 开发所需的所有基础能力。

MyBatis-Plus Boot Starter (3.5.3.1)：这是 MyBatis-Plus 为 Spring Boot 提供的自动配置启动器，引入后自动配置了 SqlSessionFactory、MapperScanner 等，开发者只需在 application.yml 中进行少量配置即可使用。

MySQL Connector/J (8.0.33)：MySQL 官方提供的 JDBC 驱动，支持 MySQL 8.0 的新特性。

jjwt (0.11.5)：Java JWT 库，提供 Token 的生成（create）、解析（parse）和验证功能。本项目引入了 jjwt-api、jjwt-impl 和 jjwt-jackson 三个子模块，分别提供 API 接口、实现和支持 Jackson 的序列化。

Spring Boot Starter Validation：虽然本项目主要依赖前端的表单验证，但引入该依赖为后续可能的后端校验提供基础。

#### 4.2.2 应用配置文件

`application.yml` 是 Spring Boot 项目的核心配置文件，本项目的主要配置如下。

服务器端口配置为 8080，这是 Spring Boot 的默认端口，便于开发和测试。

数据源连接配置指向本地 MySQL 的 `pet_hospital` 数据库，使用 Unicode 编码和 Asia/Shanghai 时区。用户名和密码需要根据实际环境修改。

MyBatis-Plus 配置中，`map-underscore-to-camel-case` 设为 true，开启数据库字段的下划线命名到 Java 实体属性的驼峰命名的自动映射（例如 `real_name` 数据库字段自动映射为 `realName` Java 属性）。`log-impl` 设为 StdOutImpl，将 MyBatis 执行的 SQL 语句输出到控制台，方便开发阶段的调试。

JWT 配置中包含 secret（签名密钥）和 expiration（过期时间，单位为毫秒，86400000 表示 24 小时）。

#### 4.2.3 包结构规划

后端代码按功能分层组织在 `com.example.demo` 包下，包含以下子包：

- `config`：存放 Spring 配置类，如 WebConfig（CORS 配置和拦截器注册）。
- `controller`：存放控制器类，每个类以 @RestController 注解标识，对应一组 RESTful API 端点。
- `entity`：存放实体类，包含 User、Doctor、Pet、Appointment、Consultation、Medicine、Order、DoctorSchedule、Notification、PrescriptionMedicine 共 10 个实体，每个以 @TableName 注解标识对应数据库中的一张表。
- `mapper`：存放 MyBatis-Plus 的 Mapper 接口，每个接口继承 BaseMapper，自动获得基础的 CRUD 方法。
- `service`：存放服务层接口和实现类（impl 子包），封装业务逻辑。
- `common`：存放通用组件，如统一返回结果封装类（Result）和全局异常处理器（GlobalExceptionHandler）。
- `util`：存放工具类，如 JWT 工具类（JwtUtil）。
- `interceptor`：存放拦截器类，如 JWT 认证拦截器（JwtInterceptor）。

### 4.3 后端核心功能实现

#### 4.3.1 JWT 认证体系

JWT 认证体系是系统安全的基础，由三个组件协作完成。

JwtUtil 工具类负责 Token 的生成和解析。生成 Token 时，使用 jjwt 提供的 Builder 模式，通过 setSubject 设置主题，claim 方法添加自定义声明（userId、username、role），setExpiration 设置过期时间（当前时间 + 配置的 expiration 值），最后使用 HS256 算法和配置的密钥进行签名。解析 Token 时，使用 jjwt 提供的 Parser，设置签名密钥后调用 parseClaimsJws 方法获得 Claims 对象，从中提取所需的声明信息。Token 过期检查通过比较过期时间和当前时间来完成。

JwtInterceptor 拦截器负责在每个请求到达 Controller 之前进行身份验证。拦截器实现 HandlerInterceptor 接口，在 preHandle 方法中编写验证逻辑。验证流程为：跳过 OPTIONS 预检请求 → 从 Header 提取 Token → 去掉 "Bearer " 前缀 → 调用 JwtUtil 检查过期 → 调用 JwtUtil 解析 → 检查路径权限 → 将用户信息注入 request 属性。

WebConfig 配置类负责将拦截器注册到 Spring MVC 的拦截器链中，并配置 CORS 跨域支持。addInterceptors 方法注册 JwtInterceptor 并指定拦截路径为 "/api/**"，排除 "/api/login" 和 "/api/register" 两个公开路径。addCorsMappings 方法配置全局 CORS，允许所有来源、所有方法和所有 Header。在生产环境中，CORS 应限制具体的来源域名，但开发阶段全开放便于调试。

登录逻辑在 AuthController 中实现。登录时根据 role 参数决定查询 user 表还是 doctor 表。为了防止密码信息泄露，User 实体和 Doctor 实体的 password 字段上都标注了 @JsonIgnore 注解，这样在序列化为 JSON 返回时，password 字段会被忽略，不会暴露在 API 响应中。

#### 4.3.2 密码加密机制

系统使用 Spring 框架自带的 DigestUtils 工具类进行 MD5 摘要计算。MD5 是一种单向哈希函数，给定一个输入字符串，它总是产生一个固定长度（128 位，通常表示为 32 个十六进制字符）的输出摘要。MD5 的特点是无法从摘要反推出原始字符串（单向性），以及不同的输入产生不同摘要的可能性极低（抗碰撞性）。

在系统中，密码加密发生在以下几个场景：用户注册时（UserServiceImpl.register）、医生注册时（DoctorServiceImpl.register）、管理端添加用户时（AdminController.addUser）、管理端添加/编辑医生时（AdminController.addDoctor/updateDoctor）、管理端编辑用户时（AdminController.updateUser，仅当密码字段非空时）、用户/医生/管理员修改密码时（各 Controller 的 changePassword 方法）。

登录验证发生在 UserServiceImpl.login 和 DoctorServiceImpl.login 中：将用户输入的密码进行 MD5 计算后，与数据库中存储的密文进行比对，一致则验证通过。

#### 4.3.3 预约状态流转实现

预约的状态流转是本系统的核心业务流程之一。状态包含五个值：PENDING（用户创建预约后的初始状态）、APPROVED（医生审核通过）、REJECTED（医生审核拒绝）、IN_PROGRESS（医生创建就诊记录后）、COMPLETED（医生完成就诊后）。

触发状态变更的操作分布在两个 Controller 中。在 UserController 中，用户创建预约时状态设为 PENDING。在 DoctorController 中，医生的 approve 操作将状态从 PENDING 变为 APPROVED；医生的 reject 操作将状态从 PENDING 变为 REJECTED；医生的 addConsultation 操作不仅创建了就诊记录（状态为 IN_PROGRESS），还同步将关联预约的状态从 APPROVED 变更为 IN_PROGRESS；医生的 completeConsultation 操作不仅将就诊记录状态从 IN_PROGRESS 变为 COMPLETED，还同步将关联预约的状态从 IN_PROGRESS 变为 COMPLETED。

预约和就诊状态同步更新的业务意图是：预约"已完成"不应该在医生创建就诊记录时就触发（因为此时只是开始看诊，还没完成），而应该等到医生对宠物完成所有检查和治疗后、点击"完成就诊"按钮时才触发。这样就准确地反映了实际的业务流程：预约 → 到院 → 就诊中 → 就诊完成。

#### 4.3.4 订单流程实现

订单的生命周期包括四个状态：UNPAID（下单后初始）、PAID（用户支付）、SHIPPED（管理员发货）、RECEIVED（用户确认收货）。

下单流程（UserController.addOrder）的关键步骤是库存检查和扣减。后端首先通过 medicineMapper.selectById 获取药品信息，检查请求的购买数量是否超过了当前库存。如果库存不足则返回错误。如果库存充足，则计算订单总价（药品单价 × 购买数量），扣减药品库存（medicine.setStock(medicine.getStock() - order.getQuantity())），然后创建订单记录（状态为 UNPAID）。整个操作在同一个方法中完成，利用数据库的事务特性保证数据一致性。

支付流程（UserController.payOrder）的关键步骤是权限校验和余额扣减。后端首先校验订单是否属于当前用户（防止用户支付他人的订单或通过修改请求参数绕过支付），然后获取用户信息，检查用户余额是否足够支付订单金额。余额不足则返回错误。余额充足则从用户余额中扣除订单金额，将订单状态改为 PAID，同时记录支付时间。在这个方法中，订单归属权的校验和余额的检查构成了双重的安全保障。

发货流程（AdminController.shipOrder）是管理端的操作。后端首先检查订单状态是否为 PAID（只有已支付的订单才能发货），不满足则返回错误。满足则将状态改为 SHIPPED，记录发货时间。这个状态检查确保了订单流程的顺序性。

收货流程（UserController.receiveOrder）是用户端的操作。后端校验订单归属权后，将状态从 SHIPPED 改为 RECEIVED，记录收货时间。

在整个订单流程中，每个环节都有明确的权限和条件限制，确保了操作的安全性和流程的正确性。

#### 4.3.5 软删除机制实现

软删除机制涉及预约表（appointment）和就诊记录表（consultation）。

在实体层面，两个实体类都增加了 deleted 字段（Integer 类型，0 表示正常，1 表示已删除）。数据表中增加同名列（TINYINT 类型，默认值 0）。

在管理端的删除操作中（AdminController），DELETE 方法不再调用 mapper.deleteById()（这会物理删除数据行），而是先通过 mapper.selectById 查出记录，将 deleted 设为 1，再通过 mapper.updateById 更新回数据库。这样数据行得以保留，只是标记为已删除。

在医生端和用户端的列表查询中（DoctorController、UserController），QueryWrapper 中添加了 `.ne(Entity::getDeleted, 1)` 条件，意思是"deleted 不等于 1"，从而过滤掉已软删除的记录。

在医生端的统计接口中（DoctorController 的 consultationStats 和 appointmentStats），统计时查询的是全量数据（不添加 deleted 条件过滤），因此软删除的记录也会被计入统计。其中"待处理预约数"（pending）是一个例外——它在统计时额外增加了 deleted != 1 的判断，因为用户和医生期望的是"待处理的数量会随着预约被处理而减少"。

这样的设计实现了"列表页看不到已删除记录，但统计数字不会因为删除而减少"的效果，既满足了数据清理的需求，又保护了统计数据的完整性。

#### 4.3.6 统一返回结果与异常处理

系统定义了一个通用的 Result 类，用于包装所有 API 接口的返回数据。Result 使用 Java 泛型，包含三个字段：code（Integer，状态码，200 表示成功）、message（String，提示信息）、data（T，泛型数据，具体类型由接口决定）。静态工厂方法 Result.success(data) 创建成功结果（code=200，message="操作成功"），Result.error(message) 创建失败结果（code=400，message 为传入的字符串）。

全局异常处理器 GlobalExceptionHandler 使用 @RestControllerAdvice 注解，捕获 Controller 层抛出的 RuntimeException 和通用 Exception。当系统发生异常时（如空指针、SQL 异常等），全局异常处理器会拦截异常并返回格式统一的错误信息，而不是让异常直接暴露给前端。这使得前端可以一致地处理错误情况，也避免了异常信息泄露到客户端。

### 4.4 前端项目搭建

#### 4.4.1 项目初始化

前端项目通过 Vite 脚手架创建，位于 `vue-admin` 目录下。创建完成后，安装项目所需的依赖包。核心依赖包括：vue（Vue 3 框架核心）、vue-router（路由管理）、pinia（状态管理）、axios（HTTP 请求库）、element-plus（UI 组件库）、@element-plus/icons-vue（图标库）。

#### 4.4.2 项目配置文件

`vite.config.js` 是 Vite 的配置文件，本项目的主要配置包括：开发服务器端口设为 3000；配置代理规则，将以 `/api` 开头的请求代理转发到 `http://localhost:8080`。这样前端在开发时请求 `/api/xxx` 实际上会被 Vite 转发到后端的 8080 端口，避免了跨域问题的困扰。

#### 4.4.3 入口文件与全局配置

`main.js` 是 Vue 应用的入口文件。它完成了以下工作：创建 Vue 应用实例；全局注册 Element Plus 组件库（并设置中文语言包）；全局注册 Element Plus 的所有图标组件（使用 `for...in` 循环批量注册）；注册 Vue Router 路由器；注册 Pinia 状态管理器；将应用挂载到 `index.html` 中的根元素上。

### 4.5 前端核心功能实现

#### 4.5.1 Axios 请求封装

`src/api/index.js` 封装了项目的 HTTP 请求模块。核心代码包含两大部分。

创建 Axios 实例：使用 axios.create 方法创建实例，设置 baseURL 为 `/api`。这意味着所有请求路径只需写相对路径（如 `/login`），自动拼接为 `/api/login`。timeout 设为 15000 毫秒（15秒），超时则请求失败。

请求拦截器：在每次请求发出前，从 localStorage 中读取 token。如果 token 存在，将其以 `Bearer <token>` 的格式设置到请求 Header 的 Authorization 字段中。这一机制实现了 Token 的自动携带，开发者无需在每个请求中手动添加。

响应拦截器：在每次收到响应后，首先检查响应的 HTTP 状态码。如果状态码是 401（未授权），说明 Token 过期或无效，此时自动清除 localStorage 中存储的 token 和 role，并使用 Vue Router 将页面重定向到登录页。对于其他错误，返回一个 rejected 的 Promise，由调用方的 catch 块处理。正常响应则直接返回 response.data（即后端返回的 Result 对象）。

这种统一的请求封装方案有两个好处：一是代码复用，避免了每个页面重复编写 Token 管理和错误处理逻辑；二是行为一致，全站的认证和错误处理保持一致。

#### 4.5.2 路由配置与权限守卫

`src/router/index.js` 定义了系统的完整路由结构。

路由表的设计：根路径 `/` 的 redirect 是一个函数，根据 localStorage 中存储的 role 动态决定跳转目标——ADMIN 跳管理端首页，DOCTOR 跳医生端首页，USER 跳用户端首页，没有 role 则跳登录页。登录和注册是两个独立的顶级路由。用户端、医生端和管理端各自是一个嵌套路由组，父路由使用各自的 Layout 布局组件，meta 中标注了该组需要的角色，children 中定义各个子功能页面的路由。所有页面组件使用动态导入（`() => import(...)`），实现按需加载，减小首屏体积。

导航守卫（router.beforeEach）：在每次路由跳转前进行检查。首先检查 token 是否存在——如果目标页面不是登录或注册页且没有 token，则跳转到登录页。其次检查角色匹配——如果目标路由的 meta.role 存在且与当前用户的 role 不一致，也跳转到登录页。其他情况放行。这个守卫确保了未登录用户无法访问任何业务页面，且不同角色的用户不会看到不属于自己角色的页面。

#### 4.5.3 状态管理

`src/store/index.js` 使用 Pinia 定义了全局的 userStore，管理登录状态。

Store 的状态（state）包含三个响应式字段：token（JWT 字符串）、user（用户信息对象）、role（角色字符串）。这些状态在应用初始化时从 localStorage 中读取默认值。

Store 的 actions 包含两个方法。setLogin 方法在登录成功后调用，将 token、user、role 保存到 Pinia 的状态中，同时通过 localStorage.setItem 持久化存储，确保即使页面刷新也不会丢失登录状态。logout 方法在退出登录时调用，清空 Pinia 状态和 localStorage 中存储的数据。

#### 4.5.4 布局组件

每种角色有一个独立的 Layout 布局组件（user/Layout.vue、doctor/Layout.vue、admin/Layout.vue）。三者的结构完全一致，只是菜单项不同。

布局使用 Element Plus 的 el-container 组件，采用经典的"左侧菜单 + 右侧内容"布局。左侧是 el-aside（宽度 220px，深色背景 #304156），内部包含系统 Logo（宠物医院-XX端）和 el-menu 导航菜单。菜单使用 router 属性，使菜单项的 index 直接对应 Vue Router 的路由路径，点击菜单项即可切换页面。菜单项配有对应的 Element Plus 图标，使用 el-icon 组件包裹。

右侧分为上下两部分：顶部是 el-header（白色背景，含问候语和退出登录按钮），下方是 el-main（浅灰背景 #f0f2f5，放置 `<router-view />` 用于渲染子路由的页面组件）。退出登录按钮点击后调用 userStore.logout() 清除登录状态并跳转到登录页。

#### 4.5.5 登录与注册页面

登录页面（Login.vue）包含用户名输入框、密码输入框、角色选择（单选按钮组，包含"用户"和"医生"两个选项，管理员身份通过用户名自动识别）和登录/注册按钮。页面底部新增"忘记密码"链接，点击跳转至 ResetPassword.vue 重置密码页面。

重置密码页面（ResetPassword.vue）采用与登录页相同的居中卡片布局。用户需填写用户名、手机号、新密码和确认新密码。提交后调用 POST /api/reset-password 接口完成密码重置。

登录流程为：用户输入用户名和密码，选择角色，点击登录。前端调用 `/login` 接口。对于用户和医生角色，前端将用户在单选按钮组中选择的 role 发送给后端。对于管理员，前端检测到用户名为 "admin" 时，自动将角色设为 ADMIN 发送给后端（因为登录页的选项中没有管理员选项，这是有意设计——管理员是系统预设的，不需要在登录界面暴露管理员选项）。

注册页面（Register.vue）包含用户名、密码、真实姓名、手机号、邮箱输入框和角色选择（用户或医生）。提交后调用 `/register` 接口完成注册，前端自动跳转到登录页。

#### 4.5.6 用户端页面实现

用户端包含 10 个页面，下面以几个典型页面为例说明实现方式。

**医生浏览与预约页面（Doctors.vue）**：页面加载时调用 `/doctors` 接口获取医生列表。医生以卡片形式展示，点击"预约"按钮弹出预约对话框（el-dialog），包含宠物名称、宠物种类、病情描述、预约时间等表单项，以及关联医生 ID 的隐藏字段。提交表单时调用 POST `/appointments` 创建预约。

**药品管理页面（Medicines.vue）**：页面加载时调用 `/medicines` 接口获取药品列表，表格展示。点击"购买"按钮弹出购买对话框，包含药品信息和数量输入框。提交时调用 POST `/orders` 创建订单，成功后自动刷新药品列表以更新库存显示。

**订单管理页面（Orders.vue）**：页面加载时调用 `/orders` 接口获取订单列表。表格中药品名称通过 medicineMap（一个在页面加载时从 `/medicines` 接口构建的 ID→名称映射对象）进行转换显示。操作按钮根据订单状态动态显示——使用了 Vue 的 `v-if` 指令控制按钮的显示和隐藏。支付按钮调用 POST `/orders/{id}/pay`，收货按钮调用 POST `/orders/{id}/receive`，删除按钮调用 DELETE `/orders/{id}`。所有操作都带有 ElMessageBox 的二次确认对话框。

#### 4.5.7 医生端页面实现

**预约管理页面（Appointments.vue）**：页面加载时调用 `/doctor/appointments` 和 `/doctor/users` 两个接口（使用 Promise.all 并行请求）。users 接口的数据用于构建 userMap 映射对象，将预约中的 userId 转换为用户真实姓名显示。待审核预约的操作列显示"通过"和"拒绝"两个按钮，分别调用 PUT `/doctor/appointments` 接口修改 status 字段。

**就诊管理页面（Consultations.vue）**：这是医生端最复杂的页面。页面加载时并行请求就诊记录列表、预约列表和用户列表三个接口。预约列表中筛选出状态为 APPROVED（已通过）的记录，作为创建就诊时的可选预约。创建就诊记录时，医生从下拉列表中选择一条已通过的预约，系统自动填充宠物名和关联的预约 ID。提交时调用 POST `/doctor/consultations`，后端自动将状态设为 IN_PROGRESS，并同步更新关联预约的状态。表格的操作列中，"完成"按钮仅在就诊状态不是 COMPLETED 时显示。点击"完成"弹出确认对话框，确认后调用 PUT `/doctor/consultations/{id}/complete`。

#### 4.5.8 管理端页面实现

**用户管理页面（Users.vue）**：页面加载时调用 `/admin/users` 获取用户列表。添加用户时弹出对话框，角色通过 el-select 下拉选择（USER 或 ADMIN）。编辑用户时，密码字段留空表示不修改密码——前端在 save 函数中判断：如果是编辑模式且密码字段为空，则从提交的数据中删除 password 字段。这一逻辑是之前修 Bug 时添加的，避免了编辑用户时把密码设为空字符串导致用户被锁定。

**药品管理页面（Medicines.vue）**：提供了完整的 CRUD 功能。表格中展示药品名称、描述、单价和库存。添加和编辑操作通过同一个 el-dialog 完成，根据当前是否有选中的记录行来判断是新增还是编辑模式。

**就诊记录管理页面（Consultations.vue）**和**预约记录管理页面（Appointments.vue）**：这两个页面在管理端是只读的（除了软删除操作）。页面展示了所有数据，不限制归属关系。对于已被软删除的记录，状态列显示红色"已删除"标签。未软删除的记录操作列显示"删除"按钮，点击后调用 DELTE 接口执行软删除。已软删除的记录不显示删除按钮。

#### 4.5.9 新增页面实现

**宠物档案页面（Pets.vue）**：页面加载时调用 /api/pets 获取当前用户的宠物列表，表格展示宠物名、种类、品种、年龄和性别。添加和编辑通过同一个 el-dialog 完成。删除时调用 DELETE /api/pets/{id}（软删除）。

**消息通知页面（Notifications.vue）**：页面加载时调用 /api/notifications 获取通知列表，按时间降序排列。未读通知以红色标签标记。提供"标记已读"单条操作和"全部已读"批量操作按钮。

**排班管理页面（Schedules.vue）**：医生使用 el-time-picker 组件选择时间，el-select 选择星期。排班列表以表格展示，包含星期、时间范围和最大接诊数。支持添加和删除操作。

**就诊记录增强（Consultations.vue - 用户端）**：在原有就诊记录表格基础上，新增了评分列（展示星级评价或"待评价"文字）、"药方"按钮（查看处方药品明细）、"评价"按钮（对已完成的就诊弹出星级评分对话框）和"购买"按钮（从处方药品一键下单）。

**医生端就诊管理增强**：在编辑就诊记录的对话框中新增药方管理区域。医生从 el-select 中选择药品，设置数量和用法用量后添加。已添加的药品以子表格形式展示，支持删除。处方药品通过 /doctor/consultations/{id}/medicines 接口进行管理。

**医生浏览页面增强（Doctors.vue）**：预约对话框中新增"选择宠物"下拉框，从用户的宠物档案中加载。选中宠物后自动填充宠物名称和种类。医生卡片新增"查看排班"按钮，点击后弹出排班表。

**ECharts 图表集成**：在三个端的首页各引入了 ECharts 图表。用户端使用饼图展示预约状态分布、柱状图展示订单状态统计；医生端使用环形图展示预约状态分布、柱状图展示评分分布；管理端使用饼图展示订单收入分布、柱状图展示平台数据概览。图表的数据来源于各端的统计接口或列表数据的前端聚合计算。

**数据导出功能**：管理端首页新增"导出预约记录"和"导出订单记录"两个按钮。点击后调用对应的导出接口，将返回的 JSON 数据转换为 CSV 格式，生成 Blob 对象并触发浏览器下载。CSV 文件开头添加 UTF-8 BOM（﻿），确保在 Microsoft Excel 中正确显示中文。

#### 4.5.10 前端组件间通信与数据流

前端各页面之间的数据通信主要通过以下方式实现。

父组件与子组件之间通过 props 传递数据，通过 emits 传递事件。在本项目中，这种场景较少，因为各页面是独立的路由组件。

跨页面/跨组件的数据共享通过 Pinia Store 实现，主要是登录用户信息（ID、用户名、角色）和 Token。任何页面都可以通过 `useUserStore()` 获取和修改这些全局状态。

页面内部的数据管理使用 Vue 3 的 `ref()` 和 `reactive()` 函数。`ref()` 用于基本类型和数组（通过 `.value` 访问），`reactive()` 用于对象。由于数据驱动视图的响应式特性，当这些变量的值发生变化时，页面会自动重新渲染。

前后端数据交互统一通过 Axios 实例进行，接口返回的 Promise 通过 `async/await` 语法处理，代码清晰易读。页面加载时的数据获取在 `onMounted` 生命周期钩子中执行，操作按钮的事件处理中调用对应的 API 接口，操作成功后重新调用数据加载函数以刷新页面展示。

---

## 第5章 系统测试

### 5.1 测试环境

| 项目 | 配置 |
|------|------|
| 服务器 | 本地开发机 |
| 后端地址 | http://localhost:8080 |
| 前端地址 | http://localhost:3000 |
| 数据库 | MySQL 8.0, pet_hospital |
| 浏览器 | Google Chrome 120 |
| 测试用数据 | 预置管理员账号、自行注册的测试用户和测试医生 |

### 5.2 测试方法

本系统采用黑盒测试方法，即从用户的角度出发，不考虑内部代码实现，只关注系统的输入和输出是否符合预期。测试范围覆盖了三种用户角色的主要功能模块，包括功能测试（验证各模块功能是否正确实现）和安全测试（验证权限控制和数据保护是否有效）。

测试用例的设计采用了等价类划分和边界值分析的方法。对于每个功能点，设计正常流程的测试用例和异常流程的测试用例。正常流程验证功能在正确输入下的表现，异常流程验证功能在错误输入或异常条件下的容错能力。

### 5.3 功能测试

#### 5.3.1 用户端功能测试

用户端测试覆盖了注册、登录、医生浏览、预约挂号、预约管理、药品浏览、订单管理、就诊记录查看、充值和密码修改等功能。

**测试用例 1：用户注册**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-USER-001 |
| 测试目的 | 验证用户注册功能是否正常 |
| 前置条件 | 系统中不存在用户名为 testuser01 的账号 |
| 测试步骤 | 1. 打开注册页面 2. 填写用户名 testuser01、密码 123456、真实姓名、手机号 3. 选择角色为"用户" 4. 点击注册按钮 |
| 预期结果 | 注册成功，页面提示注册成功 |
| 实际结果 | 注册成功，提示信息正确显示 |
| 测试结论 | 通过 |

**测试用例 2：用户登录**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-USER-002 |
| 测试目的 | 验证用户登录功能是否正常 |
| 前置条件 | 已注册 testuser01 账号 |
| 测试步骤 | 1. 打开登录页面 2. 输入用户名 testuser01、密码 123456 3. 选择"用户"角色 4. 点击登录 |
| 预期结果 | 登录成功，跳转用户端首页 |
| 实际结果 | 登录成功，页面正确跳转，首页显示用户名 |
| 测试结论 | 通过 |

**测试用例 3：预约挂号**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-USER-003 |
| 测试目的 | 验证用户预约挂号功能是否正常 |
| 前置条件 | 用户已登录，系统中存在至少一名医生 |
| 测试步骤 | 1. 进入就诊预约页面 2. 点击某位医生的"预约"按钮 3. 填写宠物名"旺财"、种类"狗" 4. 选择预约时间 5. 提交 |
| 预期结果 | 预约创建成功，状态为待审核 |
| 实际结果 | 预约创建成功，列表中新增一条待审核记录 |
| 测试结论 | 通过 |

**测试用例 4：购买药品并支付**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-USER-004 |
| 测试目的 | 验证药品购买→支付→收货流程是否正常 |
| 前置条件 | 用户已登录，药品库存>0，账户余额充足 |
| 测试步骤 | 1. 进入药品管理页面 2. 点击某药品的"购买"按钮 3. 输入数量 2 4. 确认购买 5. 进入订单管理页面 6. 点击"支付"按钮 7. 确认支付 8. 管理员发货 9. 用户点击"收货" |
| 预期结果 | 订单状态依次变化：未支付→已支付→已发货→已收货，库存和余额正确扣减 |
| 实际结果 | 各步骤执行正常，库存正确扣减 2，余额正确扣减订单金额 |
| 测试结论 | 通过 |

**测试用例 5：修改密码**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-USER-005 |
| 测试目的 | 验证密码修改功能是否正常 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 进入个人中心 2. 点击"修改密码" 3. 输入旧密码 123456、新密码 654321（两次） 4. 确认 5. 退出登录 6. 用新密码重新登录 |
| 预期结果 | 密码修改成功，旧密码无法登录，新密码可以登录 |
| 实际结果 | 密码修改成功，登录验证正确 |
| 测试结论 | 通过 |

#### 5.3.2 医生端功能测试

**测试用例 6：审核预约**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-DOCTOR-001 |
| 测试目的 | 验证医生审核预约功能是否正常 |
| 前置条件 | 医生已登录，存在分配给该医生的待审核预约 |
| 测试步骤 | 1. 进入预约管理页面 2. 对一条待审核预约点击"通过" 3. 对另一条待审核预约点击"拒绝" |
| 预期结果 | 第一条状态变为"已通过"，第二条变为"已拒绝" |
| 实际结果 | 状态变更正确，标签颜色对应正确 |
| 测试结论 | 通过 |

**测试用例 7：创建就诊记录并完成就诊**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-DOCTOR-002 |
| 测试目的 | 验证就诊创建和完成流程是否正常，预约状态是否正确联动 |
| 前置条件 | 医生已登录，存在已通过的预约 |
| 测试步骤 | 1. 进入就诊管理页面 2. 点击"添加就诊记录" 3. 选择一条已通过的预约 4. 填写诊断"感冒"、处方"口服XX" 5. 保存 → 就诊记录创建，预约状态变为就诊中 6. 点击"完成"按钮并确认→ 就诊和预约状态均变为已完成 |
| 预期结果 | 状态联动正确，预约从已通过→就诊中→已完成 |
| 实际结果 | 状态同步变更正确，时序符合设计 |
| 测试结论 | 通过 |

**测试用例 8：首页统计数据验证**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-DOCTOR-003 |
| 测试目的 | 验证医生首页的统计数据计算是否正确，特别是软删除是否不影响统计 |
| 前置条件 | 医生已登录，已有若干就诊记录和预约 |
| 测试步骤 | 1. 记录当前首页的三个数字 2. 管理员登录，软删除一条就诊记录 3. 返回医生的首页，再次查看三个数字 |
| 预期结果 | 就诊记录数和已完成数不变（累加），待处理预约数正确反映未删除的 PENDING 记录 |
| 实际结果 | 就诊记录数和已完成数保持不变，待处理预约数正确 |
| 测试结论 | 通过 |

#### 5.3.3 管理端功能测试

**测试用例 9：添加医生**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-ADMIN-001 |
| 测试目的 | 验证管理端添加医生的功能，特别是密码是否正确加密 |
| 前置条件 | 管理员已登录 |
| 测试步骤 | 1. 进入医生管理页面 2. 点击"添加医生" 3. 填写用户名 doc_test、密码 123456 等信息 4. 保存 5. 用该医生账号尝试登录 |
| 预期结果 | 添加成功，医生可以用 doc_test / 123456 登录 |
| 实际结果 | 添加成功，登录正常（说明密码已正确加密存储） |
| 测试结论 | 通过 |

**测试用例 10：编辑用户（不修改密码）**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-ADMIN-002 |
| 测试目的 | 验证编辑用户信息时密码留空不会破坏原密码 |
| 前置条件 | 管理员已登录，存在测试用户 |
| 测试步骤 | 1. 进入用户管理页面 2. 编辑某个用户，只修改手机号，密码留空 3. 保存 4. 该用户用原密码尝试登录 |
| 预期结果 | 用户仍可使用原密码登录 |
| 实际结果 | 登录成功，密码未被修改 |
| 测试结论 | 通过 |

**测试用例 11：订单发货**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-ADMIN-003 |
| 测试目的 | 验证管理端发货功能是否正常，是否只能对已支付订单发货 |
| 前置条件 | 管理员已登录，存在已支付和未支付的订单 |
| 测试步骤 | 1. 进入订单管理页面 2. 对已支付订单点击"发货" 3. 观察未支付订单是否显示发货按钮 |
| 预期结果 | 已支付订单发货成功，状态变更；未支付订单不显示发货按钮 |
| 实际结果 | 发货成功，状态正确变更；未支付订单无发货按钮 |
| 测试结论 | 通过 |

**测试用例 12：软删除预约记录**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-ADMIN-004 |
| 测试目的 | 验证软删除功能是否正常，删除后数据是否仍然存在 |
| 前置条件 | 管理员已登录，存在若干预约记录 |
| 测试步骤 | 1. 记录当前预约记录数量 2. 删除一条预约记录 3. 刷新页面观察列表变化 4. 通过数据库查询该条记录是否仍存在 |
| 预期结果 | 列表中该记录状态变为"已删除"，数据库中记录仍然存在且 deleted=1 |
| 实际结果 | 列表状态正确标记，数据库数据保留 |
| 测试结论 | 通过 |

### 5.4 安全测试

安全测试主要验证系统的三层权限控制是否有效。

**测试用例 13：未登录访问**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-SEC-001 |
| 测试目的 | 验证未登录用户是否被正确拦截 |
| 前置条件 | 清除浏览器 localStorage |
| 测试步骤 | 直接在地址栏输入 /user/dashboard |
| 预期结果 | 页面重定向到 /login |
| 实际结果 | 正确重定向 |
| 测试结论 | 通过 |

**测试用例 14：角色越权访问（前端）**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-SEC-002 |
| 测试目的 | 验证前端路由守卫是否能阻止角色越权访问 |
| 前置条件 | 以 USER 身份登录 |
| 测试步骤 | 直接在地址栏输入 /admin/dashboard |
| 预期结果 | 页面重定向到 /login（因为 USER 无 ADMIN 权限） |
| 实际结果 | 正确重定向 |
| 测试结论 | 通过 |

**测试用例 15：角色越权访问（后端）**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-SEC-003 |
| 测试目的 | 验证后端拦截器是否能阻止角色越权的 API 调用 |
| 前置条件 | 以 USER 身份登录，获取 Token |
| 测试步骤 | 使用该 Token 直接调用 GET /api/admin/users 接口 |
| 预期结果 | 返回 403 状态码，提示"无权限访问" |
| 实际结果 | 返回 403，消息为"无权限访问" |
| 测试结论 | 通过 |

**测试用例 16：数据归属权校验**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-SEC-004 |
| 测试目的 | 验证用户不能操作其他用户的数据 |
| 前置条件 | 用户 A 和用户 B 均已登录，用户 A 有一条预约 |
| 测试步骤 | 以用户 B 的身份，尝试修改用户 A 的预约（通过修改请求中的 ID） |
| 预期结果 | 返回错误，提示"无权操作" |
| 实际结果 | 返回提示"无权操作"，修改被拒绝 |
| 测试结论 | 通过 |

### 5.5 测试总结

通过上述功能测试和安全测试，系统各模块的功能均能正常运行，三种角色的权限隔离效果良好，数据保护机制（软删除、密码加密、归属权校验）都按预期工作。系统达到了设计目标，可以满足中小型宠物医院日常管理的基本需求。

**测试用例 17：忘记密码**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-MISC-001 |
| 测试目的 | 验证忘记密码重置功能是否正常 |
| 前置条件 | 已注册测试用户，知道用户名和手机号 |
| 测试步骤 | 1. 打开登录页 2. 点击"忘记密码" 3. 输入用户名、手机号、新密码 4. 提交重置 5. 用新密码登录 |
| 预期结果 | 重置成功，可用新密码登录 |
| 实际结果 | 重置和登录均正常 |
| 测试结论 | 通过 |

**测试用例 18：宠物档案管理**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-MISC-002 |
| 测试目的 | 验证宠物档案增删改查功能 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 进入宠物档案页面 2. 添加一只宠物（旺财/狗/金毛/3岁/公） 3. 编辑宠物年龄 4. 删除宠物 |
| 预期结果 | 添加成功、编辑成功、删除成功（软删除） |
| 实际结果 | 各操作正常，列表中正确显示宠物信息 |
| 测试结论 | 通过 |

**测试用例 19：就诊评价**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-MISC-003 |
| 测试目的 | 验证就诊评价功能 |
| 前置条件 | 存在已完成且未评价的就诊记录 |
| 测试步骤 | 1. 进入就诊记录页面 2. 点击"评价"按钮 3. 选择 4 星 4. 提交评价 |
| 预期结果 | 评价成功，表格中显示 4 星评分 |
| 实际结果 | 评价正常，星级显示正确 |
| 测试结论 | 通过 |

**测试用例 20：消息通知**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-MISC-004 |
| 测试目的 | 验证消息通知的自动推送和已读功能 |
| 前置条件 | 用户有预约记录 |
| 测试步骤 | 1. 医生通过该用户的预约 2. 用户登录，查看消息通知页面和未读数量 3. 标记单条为已读 |
| 预期结果 | 收到通知，未读数正确，标记已读生效 |
| 实际结果 | 通知推送及时，已读操作正常 |
| 测试结论 | 通过 |

**测试用例 21：数据导出**

| 项目 | 内容 |
|------|------|
| 测试编号 | TC-MISC-005 |
| 测试目的 | 验证管理端数据导出功能 |
| 前置条件 | 管理员已登录，存在预约和订单记录 |
| 测试步骤 | 1. 进入管理端首页 2. 点击"导出预约记录" 3. 点击"导出订单记录" |
| 预期结果 | 浏览器下载两个 CSV 文件，文件内容包含正确的关联数据 |
| 实际结果 | CSV 文件正常下载，中文显示正常，关联数据正确 |
| 测试结论 | 通过 |

测试过程中发现的问题（如管理端编辑用户密码被清空、管理端添加医生密码未加密等）已在系统实现阶段修复并进行了回归测试，确认问题已解决。

---

## 第6章 总结与展望

### 6.1 工作总结

本课题围绕宠物医院后台管理系统的设计与实现，完成了以下主要工作。

第一，完成了系统的需求分析。通过调研宠物医院的日常业务流程，梳理出三种用户角色的功能需求，确定了系统的功能边界。将需求细分为用户端的 13 个功能模块、医生端的 6 个功能模块和管理端的 9 个功能模块。

第二，完成了系统的整体设计。在技术选型上，确定了以 Spring Boot 2.7 为后端框架、Vue 3 为前端框架、ECharts 5 为图表库、MySQL 8.0 为数据库、JWT 为认证方案的技术栈。在数据库设计上，设计了 10 张核心业务表，明确了各表的字段、类型、约束和表间关系。在 API 设计上，规划了 50 余个 RESTful API 接口，覆盖三种角色全部的功能需求。在安全设计上，规划了认证安全、权限控制（三层防护）和数据保护（密码加密、软删除、归属权校验）等机制。

第三，完成了系统的编码实现。后端使用 Spring Boot + MyBatis-Plus 框架，编写了 10 个实体类、10 个 Mapper 接口、4 个 Controller 类、2 个 Service 接口和实现类，以及 JWT 工具类、JWT 拦截器和全局配置。前端使用 Vue 3 + Element Plus + ECharts 框架，编写了 29 个页面组件、路由配置、状态管理和 Axios 封装。

第四，完成了关键机制的设计与实现。包括 JWT 无状态认证与三层权限控制体系、预约→就诊→完成的状态联动机制、订单从下单到收货的完整流转流程、以及软删除与统计分离的数据保护机制。这些机制的实现使系统不仅满足了功能需求，也具备了良好的安全性和数据完整性保障。

第五，完成了系统测试。编写并执行了 21 个测试用例，覆盖了三类用户的主要功能模块、新增的 9 项扩展功能和安全机制，验证了系统的正确性和安全性。

### 6.2 系统创新点

本系统的创新点和特色主要体现在以下几个方面：

1. **软删除与统计分离机制**：通过在数据表中增加 deleted 标记字段，配合列表查询过滤和统计查询不过滤的两套查询逻辑，实现了管理端可清理数据、医生端统计数据不变的统一方案。这种设计有效解决了管理端数据维护与业务端数据统计之间的矛盾。

2. **多层权限控制体系**：系统从前端路由守卫、后端接口路径校验到数据归属权校验形成了三道防护。第一层优化用户体验，将无权限用户拦在页面之外；第二层保障接口安全，防止绕过前端的接口直接调用；第三层实现精细化权限，确保同一角色下不同用户之间的数据隔离。

3. **状态联动的业务流程设计**：预约状态与就诊状态之间存在联动关系——医生创建就诊记录时，预约自动变为"就诊中"；医生完成就诊时，就诊和预约同步变为"已完成"。这种自动化联动避免了手动维护多表状态一致性可能带来的数据不一致问题。

4. **防御性后端设计**：在支付订单和修改订单等涉及资金和状态的关键接口中，后端进行了多重的条件校验——不仅校验用户身份，还校验操作条件是否满足（如余额是否充足、订单是否处于可操作状态等）。这种设计确保了即使用户绕过前端直接调用后端接口，也无法执行非法操作。

5. **处方-购药联动设计**：通过引入处方药品表，将医生开具的药品处方与用户购药流程无缝衔接。用户可直接从就诊记录中的处方药品一键下单，无需在药品列表中重复查找，简化了从就诊到购药的业务流程。

6. **数据可视化与导出**：通过集成 ECharts 图表库，将系统的核心业务数据以可视化形式展示，帮助不同角色用户直观理解数据。管理端的数据导出功能支持生成可在 Excel 中打开的 CSV 文件，满足医院对数据的离线分析和存档需求。

### 6.3 存在的不足

尽管系统功能基本完整，但仍存在以下不足之处。

第一，密码加密强度有待提升。当前系统采用 MD5 摘要算法进行密码加密，MD5 虽然具有单向性，但存在彩虹表攻击的风险。更安全的主流做法是使用 BCrypt 或 Argon2 等带有盐值（Salt）的自适应哈希算法。这些算法不仅可以抵御彩虹表攻击，还可以通过调整迭代次数来应对硬件计算能力的提升。

第二，缺少数据分页功能。当前所有列表查询都是全量返回，在数据量较小的开发测试阶段没有问题，但如果应用于实际生产环境中（数百上千条记录），页面加载性能会明显下降。理想的方案是后端实现分页查询（利用 MyBatis-Plus 的 Page 插件），前端配合使用 Element Plus 的 Pagination 分页组件。

第三，缺少数据搜索和筛选功能。用户在管理端查看用户列表或预约记录时，如果数据较多，仅靠分页还不够，需要提供按用户名、状态、日期等条件的搜索和筛选功能。这需要后端增加查询接口的参数支持，前端添加对应的搜索表单组件。

第四，缺少操作日志功能。系统中没有记录用户的操作历史，管理员无法追溯"谁在什么时间做了什么操作"。这对于一个实际运营的医院管理系统来说是一个重要的合规性缺失。

第五，界面未做响应式设计。当前页面针对桌面端设计（最小宽度约 1200px），在平板或手机等移动设备上显示效果不佳。对于需要移动端访问的场景（如医生使用平板查房时查看预约），需要额外开发移动端适配或小程序版本。

第六，缺少集成测试和性能测试。当前只进行了手工的功能测试，没有编写自动化的单元测试和集成测试用例。也没有进行负载测试以验证系统在多并发用户下的性能表现。

### 6.4 未来展望

基于当前系统的基础和上述不足，未来的改进方向包括以下几个方面。

功能增强方面，可以增加宠物电子病历的更完整功能（支持多次就诊历史的时间线展示），增加检查报告的图片上传和管理功能，将现有的站内消息通知升级为短信或邮件通知，开发微信小程序作为移动端入口方便用户随时随地操作。

性能优化方面，可以引入 Redis 缓存热点数据（如医生列表、药品列表等查询频繁但变化较少的数据），对查询量大的字段建立数据库索引，实现后端分页和前端懒加载。

安全性提升方面，可以将密码加密算法从 MD5 升级为 BCrypt，引入 HTTPS 保障传输层安全，增加登录失败次数限制和验证码机制以防止暴力破解攻击。

运维与质量保障方面，可以编写单元测试和集成测试用例，配置 CI/CD 流水线实现自动化构建和部署，引入日志框架（如 Logback + ELK）记录和分析系统运行日志。

技术架构升级方面，可以引入 Spring Security 作为更专业的认证和授权框架（当前 JWT 拦截器是手动实现的），使用 Docker 容器化部署以简化环境配置和运维，前端引入 TypeScript 以获得更好的类型安全和开发体验。

---

## 参考文献

[1] Johnson R, Hoeller J, Arendsen A, et al. Spring Framework Reference Documentation [EB/OL]. https://docs.spring.io/spring-framework/reference/, 2023.

[2] Spring Boot Team. Spring Boot Reference Guide [EB/OL]. https://docs.spring.io/spring-boot/docs/2.7.18/reference/, 2023.

[3] MyBatis-Plus Team. MyBatis-Plus 开发文档 [EB/OL]. https://baomidou.com/pages/24112f/, 2023.

[4] 尤雨溪. Vue.js 3.0 官方文档 [EB/OL]. https://cn.vuejs.org/guide/, 2023.

[5] Element Plus Team. Element Plus 组件库文档 [EB/OL]. https://element-plus.org/zh-CN/component/, 2023.

[6] Jones M, Bradley J, Sakimura N. JSON Web Token (JWT) [S]. RFC 7519, Internet Engineering Task Force (IETF), 2015.

[7] 李刚. Spring Boot 实战派：精通 Spring Boot 2.x 与 Spring Cloud [M]. 北京：电子工业出版社, 2020: 45-128.

[8] 王福强. Spring 揭秘 [M]. 北京：人民邮电出版社, 2019: 201-256.

[9] 刘增辉. MySQL 8.0 从入门到精通 [M]. 北京：清华大学出版社, 2020: 30-180.

[10] 张耀春. Vue.js 3 企业级项目开发实战 [M]. 北京：机械工业出版社, 2022: 15-92.

[11] 陈韶健. Spring Boot 2 精髓：从构建小系统到架构分布式大系统 [M]. 北京：电子工业出版社, 2019: 56-178.

[12] Freeman E, Robson E. Head First Design Patterns [M]. 2nd ed. O'Reilly Media, 2020: 305-420.

[13] Gamma E, Helm R, Johnson R, et al. Design Patterns: Elements of Reusable Object-Oriented Software [M]. Addison-Wesley, 1994: 1-200.

[14] Fielding R T. Architectural Styles and the Design of Network-based Software Architectures [D]. University of California, Irvine, 2000.

[15] 温昱. 软件架构设计 [M]. 第2版. 北京：电子工业出版社, 2012: 88-156.
