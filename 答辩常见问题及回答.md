# 答辩常见问题及回答

## 1. 为什么选 Spring Boot + Vue 这套技术栈？

**回答思路**：Spring Boot 是 Java 体系里最主流的企业级框架，自动配置、内嵌服务器，省去很多配置工作。Vue 3 上手快，Element Plus 组件库丰富，界面好看。前后端分离是现在的主流架构——后端只负责提供数据接口，前端负责页面渲染，各干各的事，调试方便，部署也灵活。

---

## 2. JWT 认证是什么？比 Session 好在哪？

**回答**：JWT 就是在用户登录成功后，后端生成一个加密的字符串（Token）发给前端，里面存了用户 ID、用户名、角色这些信息。前端每次请求都把它放在 Header 里带过去，后端解密校验。

比 Session 好的地方：
- **无状态**：服务器不需要存 Session，扩展性好，加机器不用同步 Session
- **前后端分离友好**：前端拿 Token 就行，不需要 Cookie/Session 那一套
- **移动端也适用**：Token 是字符串，App 也能用

---

## 3. 三种角色的权限怎么控制的？

**回答**：分两层控制。

第一层是**后端 JWT 拦截器**：解析 Token 拿到角色，请求路径以 `/api/admin/` 开头的，只有 ADMIN 角色能通过，`/api/doctor/` 只有 DOCTOR 能通过，否则直接返回 403。

第二层是**前端路由守卫**：`router.beforeEach` 里检查 `localStorage` 存的角色，和路由配置的 `meta.role` 比，不匹配就跳回登录页。这层是防止用户看到不该看的页面，真正的安全在后端。

第三层是**数据归属权校验**：用户端和医生端调接口时，只能查自己的数据，比如用户只能看自己的订单、预约。后端从 Token 取出 userId，和数据库里的 `user_id` 字段比对，不匹配就拒绝。

---

## 4. 密码怎么存的？安全吗？

**回答**：密码用 MD5 摘要算法加密后存数据库，明文不落库。用户登录时把输入的密码做 MD5，和数据库比对。MD5 是单向的——知道密文推不出原文。

不过 MD5 现在不算是最高安全级别的方案（可以被彩虹表破解），更安全的做法是用 BCrypt 或加盐。这个项目作为毕业设计，MD5 够用，但我会在改进方向里提到这点。

---

## 5. 软删除是什么？为什么要用它？

**回答**：软删除就是不真正删数据，而是加一个 `deleted` 标记字段（0 正常，1 已删除），查询时过滤掉 `deleted=1` 的记录。

我们这个项目里，管理端可以删除预约和就诊记录，但医生端的首页统计数据（就诊记录总数、已完成数）需要是累加的、只增不减。因为医生需要知道自己一共看了多少病例，不能因为管理端清理数据就清零。

具体做法：
- 管理端点删除 → `deleted` 设为 1
- 医生端列表页 → 过滤 `deleted != 1`
- 医生端首页统计 → 含已删除记录，保证只增不减
- "待处理预约"这个统计 → 只计 `deleted != 1` 的 PENDING，处理完会减少

---

## 6. 预约的状态流转是怎么设计的？

**回答**：预约有 5 个状态，按流程变化：

```
PENDING（用户预约，待医生审核）
  → APPROVED（医生通过审核）
    → IN_PROGRESS（医生创建了就诊记录，正在看诊）
      → COMPLETED（医生点击"完成就诊"，预约和就诊都标记完成）
  → REJECTED（医生拒绝预约）
```

这个流转是单向的，不会回退。就诊记录创建时状态是"就诊中"，医生点"完成"后才变成"已完成"，同时关联的预约也自动变更为已完成。

---

## 7. 订单买药流程怎么保证数据正确？

**回答**：三个关键校验：

1. **下单时**：检查药品库存是否够，够了才创建订单，同时立刻扣减库存（防止超卖）
2. **支付时**：检查用户余额是否够，够了才扣款，订单状态改为"已支付"
3. **修改订单**：只有"未支付"状态的订单才能改，防止绕过支付

这些校验都在后端做，前端只是展示。即使用户绕过前端直接调接口，后端也会拦截。

---

## 8. 前后端怎么通信的？跨域怎么解决？

**回答**：前端用 Axios 发 HTTP 请求，后端返回 JSON 数据。统一封装了请求拦截器和响应拦截器——请求时自动从 localStorage 取 Token 加到 Header，响应时遇到 401 自动跳登录页。

跨域问题是因为前端跑在 `localhost:3000`，后端跑在 `localhost:8080`，不同端口也算跨域。后端配了 CORS 允许所有来源，同时 Vite 开发服务器也配了代理——`/api` 开头的请求转发到 `localhost:8080`。两种方式都用上了，开发和部署都方便。

---

## 9. MyBatis-Plus 是什么？不用写 SQL 吗？

**回答**：MyBatis-Plus 是对 MyBatis 的增强，简单的增删改查不用手写 SQL。比如：

- `userMapper.selectById(1)` → 自动生成 `SELECT * FROM user WHERE id = 1`
- `userMapper.selectList(new LambdaQueryWrapper<User>().eq(User::getRole, "ADMIN"))` → 自动生成条件查询

LambdaQueryWrapper 的好处是字段名用 Java 的 getter 方法，不会因为数据库字段改名而漏改，编译器能检查出来。

只有复杂查询才需要手写 SQL，这个项目没有复杂查询，所以全部用 MyBatis-Plus 的 API 搞定。

---

## 10. 遇到什么难点？怎么解决的？

**回答思路**（挑 2-3 个讲）：

- **角色权限控制**：一开始只做了前端路由守卫，后来发现用户可以直接调后端管理接口。修复方式是给 JWT 拦截器加了路径匹配逻辑，按 `/api/admin/` 前缀区分权限。
- **管理端删除影响统计数据**：数据库真删了，医生的首页统计就掉下来了。解决方式是引入软删除，加 `deleted` 标记字段，统计时包含已删除的记录。
- **管理端编辑用户把密码清空**：编辑时 password 字段为空字符串，MyBatis-Plus 把它也更新了，用户就无法登录。解决方式是前端判断密码为空就删掉这个字段，后端也加判断——空密码不更新。

---

## 11. 这个项目还有什么可以改进的？

**回答**：实事求是有这几点可以完善：

1. 密码加密可以从 MD5 升级到 BCrypt（更安全，自动加盐）
2. 可以加数据分页，数据多了不会一次加载所有记录
3. 可以加搜索和筛选功能
4. 就诊记录可以加图片上传（宠物照片、检查报告等）
5. 可以加操作日志，记录谁在什么时间做了什么操作
6. 可以做成响应式布局，手机上也能用

---

## 12. 数据库表之间怎么关联的？

**回答**：通过外键 ID 关联，比如：

- `appointment` 表有 `user_id` 和 `doctor_id`，能查到是谁约了哪个医生
- `consultation` 表有 `doctor_id`、`user_id`、`appointment_id`，就诊记录同时关联医生、用户和预约
- `orders` 表有 `user_id` 和 `medicine_id`，关联用户和药品

查询时没有用 JOIN，而是分两次查——先查主表，再根据 ID 列表去关联表查名字，前端组合显示。这样逻辑清晰，性能在数据量不大时也没问题。

---

## 13. 你在这个项目里的贡献是什么？

**回答**：独立完成了整个项目，包括：数据库设计（6 张表）、后端 40+ 个接口、JWT 认证和权限拦截、前端 20+ 个页面的开发。从环境搭建到功能实现到联调测试，全程自己完成。

（如果是小组项目，就说明自己负责的部分。）

---

## 14. Vue 的响应式数据是什么？怎么用的？

**回答**：Vue 3 用 `ref()` 和 `reactive()` 把普通数据变成响应式数据——数据变了，页面自动更新，不用手动操作 DOM。

- `ref()` 用于基础类型（数字、字符串），用 `.value` 读写
- `reactive()` 用于对象和数组

比如表格数据用 `const list = ref([])` 声明，调接口拿到数据后 `list.value = res.data`，表格就自动刷新了，不需要写任何 DOM 操作代码。

---

## 15. 如果用户量大了，系统怎么优化？

**回答**：
1. 加 Redis 缓存——医生列表、药品列表这种不常变的可以缓存，减少数据库压力
2. 接口加分页——现在是一次查全部，数据多了必须分页
3. 数据库加索引——在 `user_id`、`doctor_id` 这种查询频繁的字段上加索引
4. 读写分离——读多写少的场景可以用主从数据库
